[package]
name = "risc0-host"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm = { version = "3.0", features = ["prove"] }
risc0-methods = { path = "../methods" }
serde = { version = "1.0", features = ["derive"] }
