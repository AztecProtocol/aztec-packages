# Thin 'yarn install' wrapper.
set -eu

# Don't trip up GITHUB_ACTIONS output.
# Note yarn interprets CI being set as --immutable.
GITHUB_ACTIONS="" yarn install
# Make sure timestamps are reset
find . -type d -name "src" -prune -o -exec touch -d "@0" {} + 2>/dev/null || true