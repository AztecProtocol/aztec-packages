#!/usr/bin/env bash
# DO NOT output to stdout. This script is used in contexts where stdout is processed.
NO_CD=1 source ${root:-$(git rev-parse --show-toplevel)}/ci3/source

if [ "$#" -lt 2 ]; then
  echo_stderr "Usage: $0 <my-artifact.tar.gz> <binary_paths_to_tar_gz_and_upload...>"
  exit 1
fi

if [[ "$1" == *"disabled-cache"* ]]; then
  echo_stderr "Skipping upload of $1 due to uncommitted changes/files."
  exit 0
fi

# Name, intended to have .tar.gz ending
name="$1"
# Now $@ = our binary path args
shift 1

if [[ "${NO_CACHE_UPLOAD:-0}" -eq 1 ]]; then
  echo_stderr "Skipping upload because NO_CACHE_UPLOAD=1."
  exit 0
fi

# Decide whether an S3 upload will happen BEFORE tarring, so CI skips the (not free) tar of large
# artifacts that already exist remotely. No AWS calls are made at CI=0 without S3_FORCE_UPLOAD.
s3_upload=1
if [[ -z "${S3_FORCE_UPLOAD:-}" && "${CI:-0}" -eq 0 ]]; then
  echo_stderr "Skipping S3 upload because CI=0 and S3_FORCE_UPLOAD not set."
  s3_upload=0
elif [[ "${CI_SSM_MODE:-0}" -eq 0 && ( -z "${AWS_ACCESS_KEY_ID:-}" || -z "${AWS_SECRET_ACCESS_KEY:-}" ) ]] \
  && ! aws configure get aws_access_key_id &>/dev/null; then
  # In SSM/instance-profile mode, AWS CLI falls back to IMDS for credentials.
  echo_stderr "Skipping S3 upload, no AWS credentials found."
  s3_upload=0
elif [ -z "${S3_FORCE_UPLOAD:-}" ] && \
  aws ${S3_BUILD_CACHE_AWS_PARAMS:-} s3 ls "s3://aztec-ci-artifacts/build-cache/$name" &>/dev/null; then
  echo_stderr "Skipping S3 upload, already exists: $name"
  s3_upload=0
fi

# Skip tarring entirely when there is nowhere to save. Tarring large artifacts (e.g. bb build dirs)
# is not free.
if [[ "$s3_upload" -eq 0 && -z "${CACHE_LOCAL_DIR:-}" ]]; then
  echo_stderr "Skipping upload: no S3 upload to do and no CACHE_LOCAL_DIR to save into."
  exit 0
fi

out_tar=$(mktemp)
trap 'rm -f $out_tar' EXIT

if [[ "$name" == *.zst ]]; then
  tar --use-compress-program="zstd -T0" -cf $out_tar "$@"
else
  tar -czf $out_tar "$@"
fi

# Save to local cache if enabled. This happens regardless of CI/S3_FORCE_UPLOAD so that local builds
# (remote cache miss) populate $CACHE_LOCAL_DIR for reuse by other checkouts/worktrees. A local-save
# failure (e.g. read-only dir) must not fail the build, so it only warns.
if [[ -n "${CACHE_LOCAL_DIR:-}" ]]; then
  if mkdir -p "$CACHE_LOCAL_DIR" 2>/dev/null && cp "$out_tar" "$CACHE_LOCAL_DIR/$name" 2>/dev/null; then
    echo_stderr "Saved $name to local cache."
  else
    echo_stderr "Warning: Cannot create local cache dir $CACHE_LOCAL_DIR or save into it, skipping local cache."
  fi
fi

if [[ "$s3_upload" -eq 0 ]]; then
  exit 0
fi

# Pipe tar directly to AWS S3 cp
if aws ${S3_BUILD_CACHE_AWS_PARAMS:-} s3 cp $out_tar "s3://aztec-ci-artifacts/build-cache/$name" &>/dev/null; then
  echo_stderr "Cache upload of $name complete in ${SECONDS}s."
else
  echo_stderr "Cache upload of $name failed."
fi
