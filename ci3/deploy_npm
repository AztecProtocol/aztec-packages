#!/usr/bin/env bash
NO_CD=1 source $(git rev-parse --show-toplevel)/ci3/source

version=$1

dry_run=${DRY_RUN:-0}

# Only two npm dist-tags are used: 'latest' for stable releases and 'prerelease' for everything else.
# Pre-release versions (nightlies, RCs, etc.) are installed by exact version, not by dist-tag.
prerelease=$(semver prerelease $version)
if [ -n "$prerelease" ]; then
  npm_tag="prerelease"
else
  npm_tag="latest"
fi

# In a private release we publish to our internal GCP Artifact Registry instead of npmjs.org.
# gcp_artifact_login writes a token-bearing .npmrc to $GCP_NPMRC; otherwise use the npmjs.org
# .npmrc that relies on 'NPM_TOKEN' in the environment.
if [ "${PRIVATE_RELEASE:-0}" = 1 ]; then
  export GCP_NPMRC=${GCP_NPMRC:-/tmp/aztec-gcp.npmrc}
  gcp_artifact_login
  export NPM_CONFIG_GLOBALCONFIG="$GCP_NPMRC"
  npm_registry_args="--registry $INTERNAL_NPM_REGISTRY"
else
  export NPM_CONFIG_GLOBALCONFIG="$root/ci3/npm/.npmrc"
  npm_registry_args="--access public"
fi

package_name=$(jq -r '.name' package.json)
echo_header "publishing $package_name"

published_version=$(npm show . version --tag $npm_tag 2>/dev/null | grep -vE '^@' || true)

# Check if there is already a published package equal to given version, assume this is a re-run of a deploy.
if [ "$version" == "$published_version" ]; then
  echo "Tagged $npm_tag version $version is equal to published $npm_tag version $published_version."
  echo "Skipping publish."
  exit 0
fi

# Create temp directory for publishing
tmpdir=$(mktemp -d)
function cleanup {
  rm -rf "$tmpdir"
}
trap cleanup SIGINT SIGTERM EXIT

# Use npm pack instead of yarn pack. yarn pack fails on portal: dependencies (e.g. bb.js)
# because its beforeWorkspacePacking hook can't resolve workspace:^ for portal links.
# npm pack just packs the files without trying to resolve workspace refs.
npm pack --pack-destination "$tmpdir" --quiet

# Extract and prepare
cd "$tmpdir"
tar -xzf *.tgz
cd package

# Replace workspace:^ with concrete versions.
release_prep_package_json $version

# In a private release, stage the prepared tarball (with concrete versions) so the final release
# step can upload it to S3 for local verdaccio seeding / aztec-kit testing.
if [ "${PRIVATE_RELEASE:-0}" = 1 ]; then
  stage_dir=${PRIVATE_RELEASE_TARBALLS:-$root/release-tarballs}
  mkdir -p "$stage_dir"
  npm pack --pack-destination "$stage_dir" --quiet
fi

# Publish from temp dir
if [ "$dry_run" -eq 1 ]; then
  denoise "npm publish --ignore-scripts --dry-run --tag $npm_tag $npm_registry_args"
else
  # Check if version exists.
  if npm view "$package_name@$version" version >/dev/null 2>&1; then
    # Tag the existing version.
    npm dist-tag add $package_name@$version $npm_tag
  else
    # Publish new version.
    denoise "npm publish --ignore-scripts --tag $npm_tag $npm_registry_args"
  fi
fi
