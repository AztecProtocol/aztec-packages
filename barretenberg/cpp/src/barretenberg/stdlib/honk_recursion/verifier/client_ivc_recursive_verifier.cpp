#include "client_ivc_recursive_verifier.hpp"

namespace bb::stdlib::recursion::honk {

void ClientIvcRecursiveVerifier_::verify(const ClientIVC::Proof& proof)
{
    // Verify some stuff
    (void)proof;
}

} // namespace bb::stdlib::recursion::honk