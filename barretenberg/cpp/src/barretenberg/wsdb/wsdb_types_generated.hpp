// AUTOGENERATED FILE - DO NOT EDIT
// Standalone types for Wsdb service — no barretenberg dependencies.
#pragma once

// Aztec fork of msgpack-c uses THROW/RETHROW macros
#ifndef THROW
#define THROW throw
#endif
#ifndef RETHROW
#define RETHROW throw
#endif

#include <array>
#include <cstdint>
#include <msgpack.hpp>
#include <optional>
#include <string>
#include <unordered_map>
#include <vector>

/// 32-byte field element (Fr/Fq). Fixed-size, stack-allocated.
using Fr = std::array<uint8_t, 32>;

namespace bb::wsdb {

struct SiblingPathAndIndex {
    uint64_t index;
    std::vector<Fr> path;
    MSGPACK_DEFINE_MAP(index, path)
};

struct WorldStateStatusSummary {
    uint64_t unfinalizedBlockNumber;
    uint64_t finalizedBlockNumber;
    uint64_t oldestHistoricalBlock;
    bool treesAreSynched;
    MSGPACK_DEFINE_MAP(unfinalizedBlockNumber, finalizedBlockNumber, oldestHistoricalBlock, treesAreSynched)
};

struct DBStats {
    std::string name;
    uint64_t numDataItems;
    uint64_t totalUsedSize;
    MSGPACK_DEFINE_MAP(name, numDataItems, totalUsedSize)
};

struct TreeDBStats {
    uint64_t mapSize;
    uint64_t physicalFileSize;
    DBStats blocksDBStats;
    DBStats nodesDBStats;
    DBStats leafPreimagesDBStats;
    DBStats leafIndicesDBStats;
    DBStats blockIndicesDBStats;
    MSGPACK_DEFINE_MAP(mapSize,
                       physicalFileSize,
                       blocksDBStats,
                       nodesDBStats,
                       leafPreimagesDBStats,
                       leafIndicesDBStats,
                       blockIndicesDBStats)
};

struct WorldStateDBStats {
    TreeDBStats noteHashTreeStats;
    TreeDBStats messageTreeStats;
    TreeDBStats archiveTreeStats;
    TreeDBStats publicDataTreeStats;
    TreeDBStats nullifierTreeStats;
    MSGPACK_DEFINE_MAP(noteHashTreeStats, messageTreeStats, archiveTreeStats, publicDataTreeStats, nullifierTreeStats)
};

struct TreeMeta {
    std::string name;
    uint32_t depth;
    uint64_t size;
    uint64_t committedSize;
    Fr root;
    uint64_t initialSize;
    Fr initialRoot;
    uint32_t oldestHistoricBlock;
    uint32_t unfinalizedBlockHeight;
    uint32_t finalizedBlockHeight;
    MSGPACK_DEFINE_MAP(name,
                       depth,
                       size,
                       committedSize,
                       root,
                       initialSize,
                       initialRoot,
                       oldestHistoricBlock,
                       unfinalizedBlockHeight,
                       finalizedBlockHeight)
};

struct WorldStateMeta {
    TreeMeta noteHashTreeMeta;
    TreeMeta messageTreeMeta;
    TreeMeta archiveTreeMeta;
    TreeMeta publicDataTreeMeta;
    TreeMeta nullifierTreeMeta;
    MSGPACK_DEFINE_MAP(noteHashTreeMeta, messageTreeMeta, archiveTreeMeta, publicDataTreeMeta, nullifierTreeMeta)
};

struct WorldStateStatusFull {
    WorldStateStatusSummary summary;
    WorldStateDBStats dbStats;
    WorldStateMeta meta;
    MSGPACK_DEFINE_MAP(summary, dbStats, meta)
};

struct WorldStateRevision {
    uint64_t forkId;
    uint32_t blockNumber;
    bool includeUncommitted;
    MSGPACK_DEFINE_MAP(forkId, blockNumber, includeUncommitted)
};

struct WsdbGetTreeInfo {
    uint32_t treeId;
    WorldStateRevision revision;
    MSGPACK_DEFINE_MAP(treeId, revision)
};

struct WsdbGetStateReference {
    WorldStateRevision revision;
    MSGPACK_DEFINE_MAP(revision)
};

struct WsdbGetInitialStateReference {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbGetLeafValue {
    uint32_t treeId;
    WorldStateRevision revision;
    uint64_t leafIndex;
    MSGPACK_DEFINE_MAP(treeId, revision, leafIndex)
};

struct WsdbGetLeafPreimage {
    uint32_t treeId;
    WorldStateRevision revision;
    uint64_t leafIndex;
    MSGPACK_DEFINE_MAP(treeId, revision, leafIndex)
};

struct WsdbGetSiblingPath {
    uint32_t treeId;
    WorldStateRevision revision;
    uint64_t leafIndex;
    MSGPACK_DEFINE_MAP(treeId, revision, leafIndex)
};

struct WsdbGetBlockNumbersForLeafIndices {
    uint32_t treeId;
    WorldStateRevision revision;
    std::vector<uint64_t> leafIndices;
    MSGPACK_DEFINE_MAP(treeId, revision, leafIndices)
};

struct WsdbFindLeafIndices {
    uint32_t treeId;
    WorldStateRevision revision;
    std::vector<std::vector<uint8_t>> leaves;
    uint64_t startIndex;
    MSGPACK_DEFINE_MAP(treeId, revision, leaves, startIndex)
};

struct WsdbFindLowLeaf {
    uint32_t treeId;
    WorldStateRevision revision;
    Fr key;
    MSGPACK_DEFINE_MAP(treeId, revision, key)
};

struct WsdbFindSiblingPaths {
    uint32_t treeId;
    WorldStateRevision revision;
    std::vector<std::vector<uint8_t>> leaves;
    MSGPACK_DEFINE_MAP(treeId, revision, leaves)
};

struct WsdbAppendLeaves {
    uint32_t treeId;
    std::vector<std::vector<uint8_t>> leaves;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(treeId, leaves, forkId)
};

struct WsdbBatchInsert {
    uint32_t treeId;
    std::vector<std::vector<uint8_t>> leaves;
    uint32_t subtreeDepth;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(treeId, leaves, subtreeDepth, forkId)
};

struct WsdbSequentialInsert {
    uint32_t treeId;
    std::vector<std::vector<uint8_t>> leaves;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(treeId, leaves, forkId)
};

struct WsdbUpdateArchive {
    std::unordered_map<uint32_t, std::pair<std::vector<uint8_t>, uint64_t>> blockStateRef;
    Fr blockHeaderHash;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(blockStateRef, blockHeaderHash, forkId)
};

struct WsdbCommit {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbRollback {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct NullifierLeafValue {
    Fr nullifier;
    MSGPACK_DEFINE_MAP(nullifier)
};

struct PublicDataLeafValue {
    Fr slot;
    Fr value;
    MSGPACK_DEFINE_MAP(slot, value)
};

struct WsdbSyncBlock {
    uint32_t blockNumber;
    std::unordered_map<uint32_t, std::pair<std::vector<uint8_t>, uint64_t>> blockStateRef;
    Fr blockHeaderHash;
    std::vector<Fr> paddedNoteHashes;
    std::vector<Fr> paddedL1ToL2Messages;
    std::vector<NullifierLeafValue> paddedNullifiers;
    std::vector<PublicDataLeafValue> publicDataWrites;
    MSGPACK_DEFINE_MAP(blockNumber,
                       blockStateRef,
                       blockHeaderHash,
                       paddedNoteHashes,
                       paddedL1ToL2Messages,
                       paddedNullifiers,
                       publicDataWrites)
};

struct WsdbCreateFork {
    bool latest;
    uint32_t blockNumber;
    MSGPACK_DEFINE_MAP(latest, blockNumber)
};

struct WsdbDeleteFork {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbFinalizeBlocks {
    uint32_t toBlockNumber;
    MSGPACK_DEFINE_MAP(toBlockNumber)
};

struct WsdbUnwindBlocks {
    uint32_t toBlockNumber;
    MSGPACK_DEFINE_MAP(toBlockNumber)
};

struct WsdbRemoveHistoricalBlocks {
    uint32_t toBlockNumber;
    MSGPACK_DEFINE_MAP(toBlockNumber)
};

struct WsdbGetStatus {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbCreateCheckpoint {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbCommitCheckpoint {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbRevertCheckpoint {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbCommitAllCheckpoints {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbRevertAllCheckpoints {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbCopyStores {
    std::string dstPath;
    std::optional<bool> compact;
    MSGPACK_DEFINE_MAP(dstPath, compact)
};

struct WsdbShutdown {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbErrorResponse {
    std::string message;
    MSGPACK_DEFINE_MAP(message)
};

struct WsdbGetTreeInfoResponse {
    uint32_t treeId;
    Fr root;
    uint64_t size;
    uint32_t depth;
    MSGPACK_DEFINE_MAP(treeId, root, size, depth)
};

struct WsdbGetStateReferenceResponse {
    std::unordered_map<uint32_t, std::pair<std::vector<uint8_t>, uint64_t>> state;
    MSGPACK_DEFINE_MAP(state)
};

struct WsdbGetInitialStateReferenceResponse {
    std::unordered_map<uint32_t, std::pair<std::vector<uint8_t>, uint64_t>> state;
    MSGPACK_DEFINE_MAP(state)
};

struct WsdbGetLeafValueResponse {
    std::optional<std::vector<uint8_t>> value;
    MSGPACK_DEFINE_MAP(value)
};

struct WsdbGetLeafPreimageResponse {
    std::optional<std::vector<uint8_t>> preimage;
    MSGPACK_DEFINE_MAP(preimage)
};

struct WsdbGetSiblingPathResponse {
    std::vector<Fr> path;
    MSGPACK_DEFINE_MAP(path)
};

struct WsdbGetBlockNumbersForLeafIndicesResponse {
    std::vector<std::optional<uint32_t>> blockNumbers;
    MSGPACK_DEFINE_MAP(blockNumbers)
};

struct WsdbFindLeafIndicesResponse {
    std::vector<std::optional<uint64_t>> indices;
    MSGPACK_DEFINE_MAP(indices)
};

struct WsdbFindLowLeafResponse {
    bool alreadyPresent;
    uint64_t index;
    MSGPACK_DEFINE_MAP(alreadyPresent, index)
};

struct WsdbFindSiblingPathsResponse {
    std::vector<std::optional<SiblingPathAndIndex>> paths;
    MSGPACK_DEFINE_MAP(paths)
};

struct WsdbAppendLeavesResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbBatchInsertResponse {
    std::vector<uint8_t> result;
    MSGPACK_DEFINE_MAP(result)
};

struct WsdbSequentialInsertResponse {
    std::vector<uint8_t> result;
    MSGPACK_DEFINE_MAP(result)
};

struct WsdbUpdateArchiveResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbCommitResponse {
    WorldStateStatusFull status;
    MSGPACK_DEFINE_MAP(status)
};

struct WsdbRollbackResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbSyncBlockResponse {
    WorldStateStatusFull status;
    MSGPACK_DEFINE_MAP(status)
};

struct WsdbCreateForkResponse {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct WsdbDeleteForkResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbFinalizeBlocksResponse {
    WorldStateStatusSummary status;
    MSGPACK_DEFINE_MAP(status)
};

struct WsdbUnwindBlocksResponse {
    WorldStateStatusFull status;
    MSGPACK_DEFINE_MAP(status)
};

struct WsdbRemoveHistoricalBlocksResponse {
    WorldStateStatusFull status;
    MSGPACK_DEFINE_MAP(status)
};

struct WsdbGetStatusResponse {
    WorldStateStatusSummary status;
    MSGPACK_DEFINE_MAP(status)
};

struct WsdbCreateCheckpointResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbCommitCheckpointResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbRevertCheckpointResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbCommitAllCheckpointsResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbRevertAllCheckpointsResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbCopyStoresResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct WsdbShutdownResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

} // namespace bb::wsdb
