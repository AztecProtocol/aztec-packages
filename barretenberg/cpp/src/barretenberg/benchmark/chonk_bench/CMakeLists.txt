if(WASM)
    barretenberg_module(chonk_bench dsl chonk stdlib_honk_verifier stdlib_sha256 stdlib_primitives)
else()
    barretenberg_module(chonk_bench vm2_stub dsl chonk stdlib_honk_verifier stdlib_sha256 stdlib_primitives)
endif()
