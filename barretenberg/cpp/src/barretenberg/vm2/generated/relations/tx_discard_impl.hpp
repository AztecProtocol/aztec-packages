// AUTOGENERATED FILE
#pragma once

#include "barretenberg/vm2/constraining/relations/relation_macros.hpp"
#include "barretenberg/vm2/generated/relations/tx_discard.hpp"

namespace bb::avm2 {

template <typename FF_>
template <typename ContainerOverSubrelations, typename AllEntities>
void tx_discardImpl<FF_>::accumulate(ContainerOverSubrelations& evals,
                                     const AllEntities& in,
                                     [[maybe_unused]] const RelationParameters<FF_>&,
                                     [[maybe_unused]] const FF_& scaling_factor)
{
    using C = ColumnAndShifts;

    const auto tx_LAST_ROW_OF_SETUP = (FF(1) - in.get(C::tx_is_revertible)) * in.get(C::tx_is_revertible_shift);
    const auto tx_PROPAGATE_DISCARD = (FF(1) - tx_LAST_ROW_OF_SETUP) * (FF(1) - in.get(C::tx_reverted));

    {
        using View = typename std::tuple_element_t<0, ContainerOverSubrelations>::View;
        auto tmp = static_cast<View>(in.get(C::tx_discard)) * (FF(1) - static_cast<View>(in.get(C::tx_discard)));
        std::get<0>(evals) += (tmp * scaling_factor);
    }
    { // CAN_ONLY_DISCARD_IN_REVERTIBLE_PHASES
        using View = typename std::tuple_element_t<1, ContainerOverSubrelations>::View;
        auto tmp = static_cast<View>(in.get(C::tx_discard)) * (FF(1) - static_cast<View>(in.get(C::tx_is_revertible)));
        std::get<1>(evals) += (tmp * scaling_factor);
    }
    { // FAILURE_MUST_DISCARD
        using View = typename std::tuple_element_t<2, ContainerOverSubrelations>::View;
        auto tmp = static_cast<View>(in.get(C::tx_reverted)) * (FF(1) - static_cast<View>(in.get(C::tx_discard)));
        std::get<2>(evals) += (tmp * scaling_factor);
    }
    { // DISCARD_PROPAGATION
        using View = typename std::tuple_element_t<3, ContainerOverSubrelations>::View;
        auto tmp = static_cast<View>(in.get(C::tx_sel)) * CView(tx_PROPAGATE_DISCARD) *
                   (static_cast<View>(in.get(C::tx_discard_shift)) - static_cast<View>(in.get(C::tx_discard)));
        std::get<3>(evals) += (tmp * scaling_factor);
    }
}

} // namespace bb::avm2
