// AUTOGENERATED FILE
#pragma once

#include <string_view>

#include "barretenberg/relations/relation_parameters.hpp"
#include "barretenberg/relations/relation_types.hpp"
#include "barretenberg/vm2/generated/columns.hpp"

namespace bb::avm2 {

template <typename FF_> class get_contract_instanceImpl {
  public:
    using FF = FF_;

    static constexpr std::array<size_t, 6> SUBRELATION_PARTIAL_LENGTHS = { 3, 5, 2, 3, 3, 2 };

    template <typename AllEntities> inline static bool skip(const AllEntities& in)
    {
        using C = ColumnAndShifts;

        return (in.get(C::execution_sel_get_contract_instance)).is_zero();
    }

    template <typename ContainerOverSubrelations, typename AllEntities>
    void static accumulate(ContainerOverSubrelations& evals,
                           const AllEntities& in,
                           [[maybe_unused]] const RelationParameters<FF>&,
                           [[maybe_unused]] const FF& scaling_factor)
    {
        using C = ColumnAndShifts;

        const auto constants_MEM_TAG_FF = FF(0);

        { // SHOULD_GET_CONTRACT_INSTANCE
            using Accumulator = typename std::tuple_element_t<0, ContainerOverSubrelations>;
            auto tmp = (in.get(C::execution_sel_should_get_contract_instance) -
                        in.get(C::execution_sel_get_contract_instance) * in.get(C::execution_should_execute_opcode));
            tmp *= scaling_factor;
            std::get<0>(evals) += typename Accumulator::View(tmp);
        }
        {
            using Accumulator = typename std::tuple_element_t<1, ContainerOverSubrelations>;
            auto tmp = in.get(C::execution_sel_should_get_contract_instance) *
                       ((in.get(C::execution_rop_1_) * (in.get(C::execution_sel_writes_in_bounds) *
                                                            (FF(1) - in.get(C::execution_dst_offset_inv)) +
                                                        in.get(C::execution_dst_offset_inv)) -
                         FF(4294967295UL)) +
                        in.get(C::execution_sel_writes_in_bounds));
            tmp *= scaling_factor;
            std::get<1>(evals) += typename Accumulator::View(tmp);
        }
        {
            using Accumulator = typename std::tuple_element_t<2, ContainerOverSubrelations>;
            auto tmp = (in.get(C::execution_sel_opcode_error) - ((FF(1) - in.get(C::execution_sel_writes_in_bounds)) +
                                                                 (FF(1) - in.get(C::execution_is_valid_member_enum))));
            tmp *= scaling_factor;
            std::get<2>(evals) += typename Accumulator::View(tmp);
        }
        {
            using Accumulator = typename std::tuple_element_t<3, ContainerOverSubrelations>;
            auto tmp = (in.get(C::execution_selected_member) -
                        (in.get(C::execution_is_deployer) * in.get(C::execution_deployer_addr) +
                         in.get(C::execution_is_class_id) * in.get(C::execution_current_class_id) +
                         in.get(C::execution_is_init_hash) * in.get(C::execution_init_hash)));
            tmp *= scaling_factor;
            std::get<3>(evals) += typename Accumulator::View(tmp);
        }
        {
            using Accumulator = typename std::tuple_element_t<4, ContainerOverSubrelations>;
            auto tmp = (in.get(C::execution_member_write_offset) -
                        in.get(C::execution_sel_writes_in_bounds) * (in.get(C::execution_rop_1_) + FF(1)));
            tmp *= scaling_factor;
            std::get<4>(evals) += typename Accumulator::View(tmp);
        }
        {
            using Accumulator = typename std::tuple_element_t<5, ContainerOverSubrelations>;
            auto tmp =
                (in.get(C::execution_write_tag) - in.get(C::execution_sel_writes_in_bounds) * constants_MEM_TAG_FF);
            tmp *= scaling_factor;
            std::get<5>(evals) += typename Accumulator::View(tmp);
        }
    }
};

template <typename FF> class get_contract_instance : public Relation<get_contract_instanceImpl<FF>> {
  public:
    static constexpr const std::string_view NAME = "get_contract_instance";

    static std::string get_subrelation_label(size_t index)
    {
        switch (index) {
        case 0:
            return "SHOULD_GET_CONTRACT_INSTANCE";
        }
        return std::to_string(index);
    }

    // Subrelation indices constants, to be used in tests.
    static constexpr size_t SR_SHOULD_GET_CONTRACT_INSTANCE = 0;
};

} // namespace bb::avm2
