// AUTOGENERATED FILE
#pragma once

#include <string_view>

#include "barretenberg/relations/relation_parameters.hpp"
#include "barretenberg/relations/relation_types.hpp"
#include "barretenberg/vm2/generated/columns.hpp"

namespace bb::avm2 {

template <typename FF_> class precomputedImpl {
  public:
    using FF = FF_;

    static constexpr std::array<size_t, 4> SUBRELATION_PARTIAL_LENGTHS = { 3, 3, 3, 3 };

    template <typename ContainerOverSubrelations, typename AllEntities>
    void static accumulate(ContainerOverSubrelations& evals,
                           const AllEntities& in,
                           [[maybe_unused]] const RelationParameters<FF>&,
                           [[maybe_unused]] const FF& scaling_factor);
};

template <typename FF> class precomputed : public Relation<precomputedImpl<FF>> {
  public:
    static constexpr const std::string_view NAME = "precomputed";

    // Subrelation indices constants, to be used in tests.
    static constexpr size_t SR_GRANULAR_SELECTORS_ON_RANGE_16 = 3;

    static std::string get_subrelation_label(size_t index)
    {
#ifdef AVM_INCLUDE_COLUMN_INFORMATION
        switch (index) {
        case SR_GRANULAR_SELECTORS_ON_RANGE_16:
            return "GRANULAR_SELECTORS_ON_RANGE_16";
        }
#endif
        return std::to_string(index);
    }
};

} // namespace bb::avm2
