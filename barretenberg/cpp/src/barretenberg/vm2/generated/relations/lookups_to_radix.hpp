// AUTOGENERATED FILE
#pragma once

#include "../columns.hpp"
#include "barretenberg/relations/generic_lookup/generic_lookup_relation.hpp"

#include <cstddef>
#include <string_view>
#include <tuple>

namespace bb::avm2 {

/////////////////// lookup_to_radix_limb_range ///////////////////

class lookup_to_radix_limb_range_settings {
  public:
    static constexpr std::string_view NAME = "LOOKUP_TO_RADIX_LIMB_RANGE";
    static constexpr std::string_view RELATION_NAME = "to_radix";

    static constexpr size_t READ_TERMS = 1;
    static constexpr size_t WRITE_TERMS = 1;
    static constexpr size_t READ_TERM_TYPES[READ_TERMS] = { 0 };
    static constexpr size_t WRITE_TERM_TYPES[WRITE_TERMS] = { 0 };
    static constexpr size_t LOOKUP_TUPLE_SIZE = 1;
    static constexpr size_t INVERSE_EXISTS_POLYNOMIAL_DEGREE = 4;
    static constexpr size_t READ_TERM_DEGREE = 0;
    static constexpr size_t WRITE_TERM_DEGREE = 0;

    // Columns using the Column enum.
    static constexpr Column SRC_SELECTOR = Column::to_radix_sel;
    static constexpr Column DST_SELECTOR = Column::precomputed_sel_range_8;
    static constexpr Column COUNTS = Column::lookup_to_radix_limb_range_counts;
    static constexpr Column INVERSES = Column::lookup_to_radix_limb_range_inv;
    static constexpr std::array<ColumnAndShifts, LOOKUP_TUPLE_SIZE> SRC_COLUMNS = { ColumnAndShifts::to_radix_limb };
    static constexpr std::array<ColumnAndShifts, LOOKUP_TUPLE_SIZE> DST_COLUMNS = { ColumnAndShifts::precomputed_clk };

    template <typename AllEntities> static inline auto inverse_polynomial_is_computed_at_row(const AllEntities& in)
    {
        return (in._to_radix_sel() == 1 || in._precomputed_sel_range_8() == 1);
    }

    template <typename Accumulator, typename AllEntities>
    static inline auto compute_inverse_exists(const AllEntities& in)
    {
        using View = typename Accumulator::View;
        const auto is_operation = View(in._to_radix_sel());
        const auto is_table_entry = View(in._precomputed_sel_range_8());
        return (is_operation + is_table_entry - is_operation * is_table_entry);
    }

    template <typename AllEntities> static inline auto get_const_entities(const AllEntities& in)
    {
        return get_entities(in);
    }

    template <typename AllEntities> static inline auto get_nonconst_entities(AllEntities& in)
    {
        return get_entities(in);
    }

    template <typename AllEntities> static inline auto get_entities(AllEntities&& in)
    {
        return std::forward_as_tuple(in._lookup_to_radix_limb_range_inv(),
                                     in._lookup_to_radix_limb_range_counts(),
                                     in._to_radix_sel(),
                                     in._precomputed_sel_range_8(),
                                     in._to_radix_limb(),
                                     in._precomputed_clk());
    }
};

template <typename FF_>
class lookup_to_radix_limb_range_relation : public GenericLookupRelation<lookup_to_radix_limb_range_settings, FF_> {
  public:
    using Settings = lookup_to_radix_limb_range_settings;
    static constexpr std::string_view NAME = lookup_to_radix_limb_range_settings::NAME;
    static constexpr std::string_view RELATION_NAME = lookup_to_radix_limb_range_settings::RELATION_NAME;

    template <typename AllEntities> inline static bool skip(const AllEntities& in)
    {
        return in.lookup_to_radix_limb_range_inv.is_zero();
    }

    static std::string get_subrelation_label(size_t index)
    {
        if (index == 0) {
            return "INVERSES_ARE_CORRECT";
        } else if (index == 1) {
            return "ACCUMULATION_IS_CORRECT";
        }
        return std::to_string(index);
    }
};

/////////////////// lookup_to_radix_limb_less_than_radix_range ///////////////////

class lookup_to_radix_limb_less_than_radix_range_settings {
  public:
    static constexpr std::string_view NAME = "LOOKUP_TO_RADIX_LIMB_LESS_THAN_RADIX_RANGE";
    static constexpr std::string_view RELATION_NAME = "to_radix";

    static constexpr size_t READ_TERMS = 1;
    static constexpr size_t WRITE_TERMS = 1;
    static constexpr size_t READ_TERM_TYPES[READ_TERMS] = { 0 };
    static constexpr size_t WRITE_TERM_TYPES[WRITE_TERMS] = { 0 };
    static constexpr size_t LOOKUP_TUPLE_SIZE = 1;
    static constexpr size_t INVERSE_EXISTS_POLYNOMIAL_DEGREE = 4;
    static constexpr size_t READ_TERM_DEGREE = 0;
    static constexpr size_t WRITE_TERM_DEGREE = 0;

    // Columns using the Column enum.
    static constexpr Column SRC_SELECTOR = Column::to_radix_sel;
    static constexpr Column DST_SELECTOR = Column::precomputed_sel_range_8;
    static constexpr Column COUNTS = Column::lookup_to_radix_limb_less_than_radix_range_counts;
    static constexpr Column INVERSES = Column::lookup_to_radix_limb_less_than_radix_range_inv;
    static constexpr std::array<ColumnAndShifts, LOOKUP_TUPLE_SIZE> SRC_COLUMNS = {
        ColumnAndShifts::to_radix_limb_radix_diff
    };
    static constexpr std::array<ColumnAndShifts, LOOKUP_TUPLE_SIZE> DST_COLUMNS = { ColumnAndShifts::precomputed_clk };

    template <typename AllEntities> static inline auto inverse_polynomial_is_computed_at_row(const AllEntities& in)
    {
        return (in._to_radix_sel() == 1 || in._precomputed_sel_range_8() == 1);
    }

    template <typename Accumulator, typename AllEntities>
    static inline auto compute_inverse_exists(const AllEntities& in)
    {
        using View = typename Accumulator::View;
        const auto is_operation = View(in._to_radix_sel());
        const auto is_table_entry = View(in._precomputed_sel_range_8());
        return (is_operation + is_table_entry - is_operation * is_table_entry);
    }

    template <typename AllEntities> static inline auto get_const_entities(const AllEntities& in)
    {
        return get_entities(in);
    }

    template <typename AllEntities> static inline auto get_nonconst_entities(AllEntities& in)
    {
        return get_entities(in);
    }

    template <typename AllEntities> static inline auto get_entities(AllEntities&& in)
    {
        return std::forward_as_tuple(in._lookup_to_radix_limb_less_than_radix_range_inv(),
                                     in._lookup_to_radix_limb_less_than_radix_range_counts(),
                                     in._to_radix_sel(),
                                     in._precomputed_sel_range_8(),
                                     in._to_radix_limb_radix_diff(),
                                     in._precomputed_clk());
    }
};

template <typename FF_>
class lookup_to_radix_limb_less_than_radix_range_relation
    : public GenericLookupRelation<lookup_to_radix_limb_less_than_radix_range_settings, FF_> {
  public:
    using Settings = lookup_to_radix_limb_less_than_radix_range_settings;
    static constexpr std::string_view NAME = lookup_to_radix_limb_less_than_radix_range_settings::NAME;
    static constexpr std::string_view RELATION_NAME =
        lookup_to_radix_limb_less_than_radix_range_settings::RELATION_NAME;

    template <typename AllEntities> inline static bool skip(const AllEntities& in)
    {
        return in.lookup_to_radix_limb_less_than_radix_range_inv.is_zero();
    }

    static std::string get_subrelation_label(size_t index)
    {
        if (index == 0) {
            return "INVERSES_ARE_CORRECT";
        } else if (index == 1) {
            return "ACCUMULATION_IS_CORRECT";
        }
        return std::to_string(index);
    }
};

/////////////////// lookup_to_radix_limb_gt_safe_limbs_range ///////////////////

class lookup_to_radix_limb_gt_safe_limbs_range_settings {
  public:
    static constexpr std::string_view NAME = "LOOKUP_TO_RADIX_LIMB_GT_SAFE_LIMBS_RANGE";
    static constexpr std::string_view RELATION_NAME = "to_radix";

    static constexpr size_t READ_TERMS = 1;
    static constexpr size_t WRITE_TERMS = 1;
    static constexpr size_t READ_TERM_TYPES[READ_TERMS] = { 0 };
    static constexpr size_t WRITE_TERM_TYPES[WRITE_TERMS] = { 0 };
    static constexpr size_t LOOKUP_TUPLE_SIZE = 1;
    static constexpr size_t INVERSE_EXISTS_POLYNOMIAL_DEGREE = 4;
    static constexpr size_t READ_TERM_DEGREE = 0;
    static constexpr size_t WRITE_TERM_DEGREE = 0;

    // Columns using the Column enum.
    static constexpr Column SRC_SELECTOR = Column::to_radix_sel;
    static constexpr Column DST_SELECTOR = Column::precomputed_sel_range_8;
    static constexpr Column COUNTS = Column::lookup_to_radix_limb_gt_safe_limbs_range_counts;
    static constexpr Column INVERSES = Column::lookup_to_radix_limb_gt_safe_limbs_range_inv;
    static constexpr std::array<ColumnAndShifts, LOOKUP_TUPLE_SIZE> SRC_COLUMNS = {
        ColumnAndShifts::to_radix_limb_index_safe_limbs_diff
    };
    static constexpr std::array<ColumnAndShifts, LOOKUP_TUPLE_SIZE> DST_COLUMNS = { ColumnAndShifts::precomputed_clk };

    template <typename AllEntities> static inline auto inverse_polynomial_is_computed_at_row(const AllEntities& in)
    {
        return (in._to_radix_sel() == 1 || in._precomputed_sel_range_8() == 1);
    }

    template <typename Accumulator, typename AllEntities>
    static inline auto compute_inverse_exists(const AllEntities& in)
    {
        using View = typename Accumulator::View;
        const auto is_operation = View(in._to_radix_sel());
        const auto is_table_entry = View(in._precomputed_sel_range_8());
        return (is_operation + is_table_entry - is_operation * is_table_entry);
    }

    template <typename AllEntities> static inline auto get_const_entities(const AllEntities& in)
    {
        return get_entities(in);
    }

    template <typename AllEntities> static inline auto get_nonconst_entities(AllEntities& in)
    {
        return get_entities(in);
    }

    template <typename AllEntities> static inline auto get_entities(AllEntities&& in)
    {
        return std::forward_as_tuple(in._lookup_to_radix_limb_gt_safe_limbs_range_inv(),
                                     in._lookup_to_radix_limb_gt_safe_limbs_range_counts(),
                                     in._to_radix_sel(),
                                     in._precomputed_sel_range_8(),
                                     in._to_radix_limb_index_safe_limbs_diff(),
                                     in._precomputed_clk());
    }
};

template <typename FF_>
class lookup_to_radix_limb_gt_safe_limbs_range_relation
    : public GenericLookupRelation<lookup_to_radix_limb_gt_safe_limbs_range_settings, FF_> {
  public:
    using Settings = lookup_to_radix_limb_gt_safe_limbs_range_settings;
    static constexpr std::string_view NAME = lookup_to_radix_limb_gt_safe_limbs_range_settings::NAME;
    static constexpr std::string_view RELATION_NAME = lookup_to_radix_limb_gt_safe_limbs_range_settings::RELATION_NAME;

    template <typename AllEntities> inline static bool skip(const AllEntities& in)
    {
        return in.lookup_to_radix_limb_gt_safe_limbs_range_inv.is_zero();
    }

    static std::string get_subrelation_label(size_t index)
    {
        if (index == 0) {
            return "INVERSES_ARE_CORRECT";
        } else if (index == 1) {
            return "ACCUMULATION_IS_CORRECT";
        }
        return std::to_string(index);
    }
};

} // namespace bb::avm2
