// AUTOGENERATED FILE - DO NOT EDIT
// Standalone types for Cdb service — no barretenberg dependencies.
#pragma once

// Aztec fork of msgpack-c uses THROW/RETHROW macros
#ifndef THROW
#define THROW throw
#endif
#ifndef RETHROW
#define RETHROW throw
#endif

#include <array>
#include <cstdint>
#include <msgpack.hpp>
#include <optional>
#include <string>
#include <unordered_map>
#include <vector>

/// 32-byte field element (Fr/Fq). Fixed-size, stack-allocated.
using Fr = std::array<uint8_t, 32>;

namespace bb::cdb {

struct CdbGetContractInstance {
    Fr address;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(address, forkId)
};

struct CdbGetContractClass {
    Fr classId;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(classId, forkId)
};

struct CdbGetBytecodeCommitment {
    Fr classId;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(classId, forkId)
};

struct CdbGetDebugFunctionName {
    Fr address;
    Fr selector;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(address, selector, forkId)
};

struct ContractClassLogFields {
    std::vector<Fr> fields;
    MSGPACK_DEFINE_MAP(fields)
};

struct ContractClassLog {
    Fr contractAddress;
    ContractClassLogFields fields;
    uint32_t emittedLength;
    MSGPACK_DEFINE_MAP(contractAddress, fields, emittedLength)
};

struct PrivateLog {
    std::vector<Fr> fields;
    uint32_t emittedLength;
    MSGPACK_DEFINE_MAP(fields, emittedLength)
};

struct ContractDeploymentData {
    std::vector<ContractClassLog> contractClassLogs;
    std::vector<PrivateLog> privateLogs;
    MSGPACK_DEFINE_MAP(contractClassLogs, privateLogs)
};

struct CdbAddContracts {
    ContractDeploymentData contractDeploymentData;
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(contractDeploymentData, forkId)
};

struct CdbCreateCheckpoint {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct CdbCommitCheckpoint {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct CdbRevertCheckpoint {
    uint64_t forkId;
    MSGPACK_DEFINE_MAP(forkId)
};

struct ContractClass {
    Fr id;
    Fr artifactHash;
    Fr privateFunctionsRoot;
    std::vector<uint8_t> packedBytecode;
    MSGPACK_DEFINE_MAP(id, artifactHash, privateFunctionsRoot, packedBytecode)
};

struct CdbAddContractClass {
    ContractClass contractClass;
    Fr bytecodeCommitment;
    MSGPACK_DEFINE_MAP(contractClass, bytecodeCommitment)
};

struct GrumpkinPoint {
    Fr x;
    Fr y;
    MSGPACK_DEFINE_MAP(x, y)
};

struct PublicKeys {
    GrumpkinPoint masterNullifierPublicKey;
    GrumpkinPoint masterIncomingViewingPublicKey;
    GrumpkinPoint masterOutgoingViewingPublicKey;
    GrumpkinPoint masterTaggingPublicKey;
    MSGPACK_DEFINE_MAP(masterNullifierPublicKey,
                       masterIncomingViewingPublicKey,
                       masterOutgoingViewingPublicKey,
                       masterTaggingPublicKey)
};

struct ContractInstance {
    Fr salt;
    Fr deployer;
    Fr currentContractClassId;
    Fr originalContractClassId;
    Fr initializationHash;
    PublicKeys publicKeys;
    MSGPACK_DEFINE_MAP(salt, deployer, currentContractClassId, originalContractClassId, initializationHash, publicKeys)
};

struct CdbAddContractInstance {
    Fr address;
    ContractInstance instance;
    MSGPACK_DEFINE_MAP(address, instance)
};

struct CdbRegisterFunctionSignatures {
    std::vector<std::string> signatures;
    MSGPACK_DEFINE_MAP(signatures)
};

struct CdbGetContractClassIds {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbShutdown {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbErrorResponse {
    std::string message;
    MSGPACK_DEFINE_MAP(message)
};

struct CdbGetContractInstanceResponse {
    std::optional<ContractInstance> instance;
    MSGPACK_DEFINE_MAP(instance)
};

struct CdbGetContractClassResponse {
    std::optional<ContractClass> contractClass;
    MSGPACK_DEFINE_MAP(contractClass)
};

struct CdbGetBytecodeCommitmentResponse {
    std::optional<Fr> commitment;
    MSGPACK_DEFINE_MAP(commitment)
};

struct CdbGetDebugFunctionNameResponse {
    std::optional<std::string> name;
    MSGPACK_DEFINE_MAP(name)
};

struct CdbAddContractsResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbCreateCheckpointResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbCommitCheckpointResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbRevertCheckpointResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbAddContractClassResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbAddContractInstanceResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbRegisterFunctionSignaturesResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

struct CdbGetContractClassIdsResponse {
    std::vector<Fr> classIds;
    MSGPACK_DEFINE_MAP(classIds)
};

struct CdbShutdownResponse {

    template <typename Packer> void msgpack_pack(Packer& pk) const { pk.pack_map(0); }
    void msgpack_unpack(msgpack::object const&) {}
};

} // namespace bb::cdb
