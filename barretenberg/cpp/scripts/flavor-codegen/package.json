{
  "name": "@aztec/flavor-codegen",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript codegen for Barretenberg flavor headers (non-AVM). Reads canonical entity orderings + relation manifests + flavor specs and emits barretenberg/cpp/src/barretenberg/flavor/generated/*.hpp.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "regenerate": "node dist/main.js",
    "regenerate:dev": "tsc && node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  }
}
