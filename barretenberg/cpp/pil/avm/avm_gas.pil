
namespace avm_gas(256);
    // TOOD: WILL BE FIXED - we should be able to have this be a fixed column / the lookup tables are fixed so require no selectors
    pol commit add_to_gas_lookup_table;

    // TODO(ISSUE_NUMBER): Constrain variable gas costs
    pol commit l2_gas_fixed_table;
    pol commit da_gas_fixed_table;