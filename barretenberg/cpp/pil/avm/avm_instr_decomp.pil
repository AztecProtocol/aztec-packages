
namespace avm_instr_decomp(256);
    // The row number ("clk") will be the implicit PC.
    pol commit decomposition_sel; // do we need one? I want all the table
    pol commit opcode_val;
    pol commit indirect;

    // raw operands
    pol commit o1;
    pol commit o2;
    pol commit o3;
    pol commit o4;
    pol commit o5;
    pol commit o6;
    pol commit o7;
    pol commit o8;