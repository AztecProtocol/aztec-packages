{
  "name": "@aztec/barretenberg-ts-packages",
  "packageManager": "yarn@4.13.0",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach -A -p -v run build",
    "clean": "yarn workspaces foreach -A -p -v run clean"
  },
  "workspaces": [
    "bb.js",
    "bb-avm-sim",
    "bb-avm-sim/packages/*",
    "cdb"
  ],
  "resolutions": {
    "@aztec/ipc-runtime": "portal:../../ipc-runtime/ts"
  }
}
