// AUTOGENERATED FILE - DO NOT EDIT
#pragma once

#include "echo_execute.hpp"
#include "barretenberg/ipc/ipc_server.hpp"
#include "barretenberg/serialize/msgpack.hpp"

#include <cstdint>
#include <functional>
#include <span>
#include <string>
#include <vector>

namespace echo {

/**
 * @brief Type alias for the dispatch function.
 *
 * Takes a EchoRequest& and a EchoCommand&& and returns a EchoCommandResponse.
 * This is typically the top-level `echo()` function from echo_execute.hpp.
 */
using EchoDispatchFn = std::function<EchoCommandResponse(EchoRequest&, EchoCommand&&)>;

/**
 * @brief Create an IPC server handler from a dispatch function.
 *
 * Returns a lambda compatible with ipc::IpcServer::run(). The handler:
 *   1. Deserializes the msgpack request: [[CommandName, {payload}]]
 *   2. Detects shutdown commands (EchoShutdown)
 *   3. Dispatches to the provided function
 *   4. Serializes the response
 *   5. Wraps exceptions in EchoErrorResponse
 */
ipc::IpcServer::Handler make_echo_handler(
    EchoRequest& request,
    EchoDispatchFn dispatch);

} // namespace echo
