- Do not change optimization levels for compile-time improvements; keep work focused on structural C++ changes backed by compiler profiling.
- Do not alter test code for compile-time improvements; use test builds only as a proxy for `ninja bb` compile time.
- PCH is not a serious primary path for this repo. It can hide repeated parsing in warm local builds, but it does not reduce semantic work, template instantiation work, constant evaluation, or invalidation churn.
- Consider PCH last, after structural reductions. The one good candidate seen so far is a narrow, target-local PCH for `bbapi_objects` over schema/serde-heavy headers (`bbapi_execute.hpp`, `msgpack_impl.hpp`): default clean `ninja bb` included PCH compilation and still dropped from ~47.4s stock to ~28.4s current, while the no-PCH diagnostic still dropped from ~46.7s to ~28.6s. Treat this as final polish, not proof of a durable main strategy.
- Move the next compile-time pass away from constants and toward compiler object volume: AST type count, declaration count, statement/expression count, source-location entries, token ownership, include graph shape, and duplicated template instantiations across TUs.
- Use Clang 20/Zig Clang as an analysis compiler, not a production semantics change. Useful knobs: `-ftime-trace-verbose`, `-Xclang -print-stats`, `-Xclang -dump-tokens`, `clang-include-cleaner`, and `clang-scan-deps`.
- Use GCC only as an independent sampling oracle for phase time and memory (`-ftime-report`, `-ftime-report-details`, `-fmem-report`), not as a build-system migration unless the data shows a real structural difference.
- Consider Templight only for one or two narrow Sumcheck/Honk repro TUs if Clang time traces cannot explain template recursion roots. It is the closest tool to "a compiler meant for this", but likely too heavy to make the normal workflow depend on it.
- Treat Sumcheck/Honk as the next heavy-work target set. Current Clang 20 sample shows `sumcheck_prover.cpp` and `sumcheck_verifier.cpp` as frontend and backend heavy, with millions of AST nodes across just the top Sumcheck TUs.
- Prioritize headers that dominate tokens and semantic volume in hot Sumcheck TUs: `poseidon2_params.hpp`, `tuplet.hpp`, `biggroup_impl.hpp`, `element_impl.hpp`, `bigfield.hpp`, `biggroup.hpp`, `univariate.hpp`, `ecc_ops_table.hpp`, and field implementation headers.
- For include cleanup, do not auto-apply `clang-include-cleaner`. Use it as a candidate generator, then measure before/after total compiler work because transitive include fixes can increase direct includes while reducing semantic drag.
- ACIR/msgpack generated-header direction: stop treating `serde/acir.hpp` as a public template-heavy serialization header. `scripts/serde_header_shape_explorer.py` measures generated header shapes in temporary include roots. Current `acir.hpp` syntax-only include cost is ~5.46s, 310 KiB, 9286 lines, 131 pack/unpack methods each, 353 `Serializable` and 353 `Deserializable` references. A true declarations-only ACIR header with a tiny `serde_light.hpp` and no `msgpack_impl.hpp` measured ~0.89s, 25.9 KiB, 1429 lines. This suggests the durable path is to generate a cheap public type header plus private decoder/encoder `.cpp` files, or a table-driven decoder, not to tune the current inline msgpack method bodies.
- Intermediate ACIR/msgpack wins from the same script: moving generated msgpack pack/unpack bodies out of the header measured ~3.96s; removing them entirely measured ~3.84s; removing serde trait specializations measured ~4.78s. Removing `msgpack_impl.hpp` and the generated helper block is the big multiplier after stripping serialization bodies.
- ACIR/msgpack quarantine result: `acir_to_constraint_buf.hpp` is now a byte-buffer API header, with generated `Acir::*`/`Witnesses::*` helpers moved to `acir_to_constraint_buf_internal.hpp`. A clean no-PCH trace build of `bb` after this showed `1628.3s` execute / `1176.4s` frontend / `414.7s` backend, down from the prior no-PCH trace sample of `1853.4s` / `1334.1s` / `474.2s`. Generated ACIR serde appears in exactly one traced TU: `acir_to_constraint_buf.cpp`.
- Msgpack rewrite direction: follow the upb/mini-table lesson, not the full C++ protobuf lesson. Generate compact schema tables or declarations, keep parsing/packing in one or a few `.cpp` files, and make ordinary C++ callers consume typed byte-level APIs or cheap structs. Cap'n Proto and FlatBuffers show another useful pattern: generated headers can expose lightweight readers/builders or object declarations while bulk encode/decode mechanics live behind generated implementation files. Useful references: `https://pigweed.googlesource.com/third_party/github/protocolbuffers/protobuf/+/HEAD/docs/upb/vs-cpp-protos.md`, `https://capnproto.org/cxx.html`, `https://flatbuffers.dev/languages/cpp/`.
- Rejected msgpack header split experiment: separating schema/cbind headers from `msgpack_impl.hpp` without removing the template runtime path increased clean no-PCH work. The refined split measured `37.80s` real / `1680.92s` user and `2168.0s` traced execute / `1565.0s` frontend / `541.6s` backend, versus the restored tree at `34.10s` real / `1524.08s` user and `1692.1s` / `1219.1s` / `433.6s`. Do not repeat a facade-only split; it hides `check_memory_span.hpp` but still makes clang instantiate msgpack-c and `NamedUnion` machinery.
- Successful msgpack compiler-friendliness pattern: move real serialization bodies out of public headers while leaving cheap field declarations in headers. Out-of-lining native Chonk proof msgpack helpers from `chonk_proof.hpp` into `chonk_proof.cpp`, plus direct includes at actual msgpack/std::format users, reduced clean no-PCH `bb` to `29.60s` real / `1443.22s` user and `1578.2s` traced execute / `1139.2s` frontend / `402.6s` backend. `msgpack_impl.hpp` exposure dropped from 35 TUs to 19, and msgpack/serde source-event cost dropped from `395.1s` to `212.9s`. Tests: `serialize-tests_tests`, `bbapi_tests`, and `chonk_tests` passed.
- Kept small msgpack include-prune: `stdlib_circuit_builders/circuit_builder_base_impl.hpp` did not use msgpack and no longer includes `msgpack_impl.hpp`. Second clean no-PCH sample after this was `28.11s` real / `1434.49s` user and `1573.0s` traced execute / `1137.8s` frontend / `399.6s` backend. `msgpack_impl.hpp` exposure dropped to 18 TUs and msgpack/serde source-event cost to `204.5s`. This is a small durable win, not a new strategy.
- Kept msgpack facade split: `msgpack.hpp` now only exposes `SERIALIZATION_FIELDS`/`NVP`; concept helpers, `msgpack::sbuffer` declarations, and camel-case field support are included only where used. A clean no-PCH `ninja bb` sample after this was `27.45s` real / `1376.72s` user, down from the previous kept `28.11s` / `1434.49s`. A syntax-only include micro-test for a tiny `SERIALIZATION_FIELDS` type dropped old `msgpack.hpp` from ~`0.31s` real to ~`0.03s`. This confirms that ordinary type headers can be made much cheaper even before a full table-driven rewrite.
- Rejected JSON-output rewrite experiment: removing msgpack from `api/json_output.hpp` and replacing the JSON formatting detour with direct `nlohmann::ordered_json` reduced msgpack source-events but did not reduce total compiler work. The direct-header variant measured `1582.1s` traced execute / `1141.8s` frontend / `404.7s` backend, and the out-of-line `json_output.cpp` variant measured `1600.5s` / `1158.9s` / `404.4s` on the clean no-regeneration sample. Do not keep “move to .cpp” changes unless the replacement implementation is also compiler-cheap.
- Next msgpack rewrite should make this out-of-line pattern systematic: headers can keep `SERIALIZATION_FIELDS`-style metadata only if it does not include `msgpack_impl.hpp` or `<msgpack.hpp>`, and every actual pack/unpack/schema body should live in a `.cpp` or generated implementation TU. Remaining compiler work is concentrated in `bbapi` command/schema TUs and the single ACIR decode TU.
- The research-backed target shape for a full msgpack rewrite is table-driven, not C++ reflection/template-driven. upb’s design is the clearest model: generated files emit compact schema tables and the runtime parser/serializer interprets those tables, instead of generating parse/serialize functions per type. Cap’n Proto’s C++ docs support the same compile-time principle: generated `.capnp.c++` files contain embedded schema data and avoid STL-heavy public interfaces. FlatBuffers’ mini-reflection is a useful lower-overhead schema-data pattern; it adds tiny per-field tables that generic visitors can consume. For bb, that means generated per-type metadata arrays plus hand-written/simple generated encode/decode loops, not `msgpack::adaptor`, `std::variant` megatypes, or schema construction through `std::make_unique<T>()`.
- For `bbapi`, replace the current `NamedUnion<...>`/`std::variant`/`std::visit`/msgpack-c conversion path with a generated command table: command name string, decode function pointer, execute function pointer, encode response function pointer, and schema descriptor pointer. This should avoid instantiating one giant command variant and the recursive `construct_by_index` chain in `c_bind.cpp`; current traces still show repeated 3-4s `NamedUnion<bbapi...>` instantiation events in that one TU.
- For schema generation, stop deriving JSON schema by constructing arbitrary `std::make_unique<T>()` objects through template overloads in C++. Generate schema data directly from the command/type metadata, either as constexpr tables with a tiny printer or as a generated JSON/msgpack blob. This attacks `bbapi_schema*.cpp` and avoids using runtime msgpack conversion templates to compute schema.
- For msgpack-c, aim to confine the umbrella `<msgpack.hpp>` include to a small set of implementation files. If we rewrite all msgpack, prefer a tiny internal reader/writer over msgpack-c adaptors for command payloads: explicit `read_map_field`, `read_array`, `read_bin`, `write_map`, `write_bin`, plus generated per-type functions. The goal is boring C++ functions and tables, not generic adaptor specialization.
- For Noir/ACIR, generate a public type-only/light serde header plus private decode/encode `.cpp` files or a minitable interpreter. The current type-shape experiment suggests this is the largest remaining generated-header opportunity: ~5.46s syntax-only for current `acir.hpp` versus ~0.89s for type-only/light serde.
- Added `scripts/serialization_rewrite_planner.py` to inventory the exact rewrite surface from a trace build plus source scan. Latest no-PCH trace report: `208` trace files, `1583.8s` execute / `1137.9s` frontend / `408.6s` backend, `130` msgpack-exposed TUs, `1` ACIR-serde-exposed TU. Source-event groups: third-party msgpack `100.28s`, bb msgpack internals `58.13s`, `msgpack_impl.hpp` `35.24s`, ACIR generated `4.70s`.
- ACIR generator inventory from the planner: `131` generated records (`58` plain structs, `17` variant wrappers, `26` newtypes, `30` unit structs), `222` field slots, `100` variant alternatives, `131` generated pack methods, `131` generated unpack methods, `353` `Serializable` refs, and `353` `Deserializable` refs. This is small enough for a generated descriptor-table implementation; it does not need C++ reflection-style templates in public headers.
- Repo-wide serialization inventory from the planner: `351` `SERIALIZATION_FIELDS`/`MSGPACK_FIELDS` uses, `820` macro field references, `42` files mentioning `msgpack_impl.hpp`, `56` mentioning `msgpack.hpp`, and four `NamedUnion` aliases. The biggest command-dispatch offenders are `bbapi::Command` (`62` alternatives) and `bbapi::CommandResponse` (`63` alternatives), followed by WSDB command/response (`30`/`31` alternatives).
- Rejected `NamedUnion` out-of-line split: moving `NamedUnion` msgpack bodies into a separate implementation header looked structurally attractive but increased clean no-PCH user CPU. The templated-proxy version measured `28.55s` real / `1416.85s` user; the exact `msgpack::object const&` version measured `27.40s` real / `1402.97s` user, both worse than the committed baseline `27.45s` / `1376.72s`. Do not split only method bodies; replace the giant variant dispatch with a generated command table.
- Kept `bbapi` schema hardcode as an intentionally narrow compiler-work reduction: `get_msgpack_schema_as_json()` now returns the captured JSON literal, and the schema walker TUs are empty placeholders. Clean no-PCH `bb` sample was `28.75s` real / `1350.34s` user versus the prior committed `27.45s` / `1376.72s`; the trace sample was `1580.9s` execute / `1120.0s` frontend / `421.0s` backend versus `1583.8s` / `1137.9s` / `408.6s`. Msgpack/serde source-event cost dropped from `203.1s` to `160.6s`, and msgpack-exposed TUs from `130` to `124`. The schema stdout remained byte-for-byte identical at `15740` bytes. This is a stopgap; durable follow-up should generate the literal or schema tables from command metadata and test against the old walker.
- Kept first table-driven `bbapi` command-dispatch rewrite. `c_bind.hpp` is now declaration-only, the direct C++ `bbapi(Command&&)` wrapper lives beside `execute`, and the msgpack IPC/WASM path decodes `[command-name, payload]` through a simple descriptor table instead of converting through the giant `Command` `NamedUnion`. Clean no-PCH sample after this was `28.67s` real / `1365.80s` user; the trace sample dropped to `1523.9s` execute / `1101.6s` frontend / `387.5s` backend from the schema-only `1580.9s` / `1120.0s` / `421.0s`. Msgpack-exposed execute dropped from `1281.0s` to `1187.2s`, and msgpack-exposed TUs from `124` to `122`. The remaining `NamedUnion` work is now mostly the direct C++ API in `bbapi_execute.cpp`; the IPC/WASM path no longer needs it.
- Rejected narrow concrete `bbapi_chonk` msgpack methods: out-of-lining only `ChonkStart`, `ChonkAccumulate`, `ChonkProve`, and touched responses required either losing schema-map output or changing the schema concept to probe `MsgpackSchemaPacker`. The semantics-preserving version built and passed `bbapi_tests`, but clean no-PCH trace work regressed to `1581.0s` execute / `1137.1s` frontend / `405.4s` backend, and msgpack-exposed work regressed to `1245.3s` execute / `898.9s` frontend with `166.5s` msgpack source-event cost. Do not keep piecemeal concrete command methods unless they remove an include/variant/schema path from other TUs; otherwise they just move work into `bbapi_chonk.cpp`.
- Added `scripts/template_layer_explorer.py` to group Clang `-ftime-trace` `Instantiate*` events by semantic layer, normalized symbol, parent source header, parent context, and TU. It reports both inclusive instantiation time and child-exclusive instantiation time; use child-exclusive to avoid being fooled by nested template frames, and use parent source/context to choose refactors.
- Current clean no-PCH template-layer baseline after reverting the narrow Chonk experiment: `1468.3s` execute / `1055.8s` frontend / `379.2s` backend. Template instantiation events sum to `1466.7s` inclusive / `427.9s` child-exclusive across `500,963` events. Top semantic layers by inclusive instantiation are `other std` (`400.2s`), `field/curve` (`384.1s`), `std vector` (`165.5s`), `circuit builder` (`82.7s`), `flavor` (`69.6s`), `msgpack` (`67.9s`), `stdlib bigfield` (`50.4s`), `relations` (`46.3s`), and `bbapi named_union/variant` (`37.3s`, all in `bbapi_execute.cpp`).
- The template-layer profile changes the next target set: stop looking only at msgpack. Repeated STL/type-trait work is being induced by Barretenberg headers. Top parent sources for instantiation are `op_queue/ecc_ops_table.hpp` (`60.2s` incl / `15.9s` self, 126 TUs), `eccvm/eccvm_flavor.hpp` (`38.1s` / `10.2s`, 53 TUs), `op_queue/ecc_op_queue.hpp` (`31.1s` / `8.6s`, 126 TUs), `field_conversion.hpp` (`27.8s` / `8.1s`, 87 TUs), `plookup_tables/types.hpp` (`24.8s` / `8.2s`, 128 TUs), `common/serialize.hpp` (`24.2s` / `6.7s`, 161 TUs), and `common/bb_bench.hpp` (`19.5s` / `6.2s`, 148 TUs).
- Concrete next refactor candidates from the template-layer profile:
  1. Remove the direct C++ `bbapi::Command` `std::visit` hot path or replace it with generated/manual switch dispatch. `bbapi_execute.cpp` is the top instantiation TU (`57.5s` inst incl / `6.4s` self) and the remaining `bbapi named_union/variant` layer is concentrated there.
  2. Move `UltraEccOpsTable`/`EccOpsTable<UltraOp>` vector insertion and reconstruction bodies out of `op_queue/ecc_ops_table.hpp`, or replace the two `EccOpsTable<T>` instantiations with concrete tables. Duplicates include `std::vector<bb::UltraOp>::insert` and `_M_range_insert` in 126 TUs.
  3. Done in the later lookup-table pass: hide `LookupHashTable`'s `std::unordered_map<std::array<fr,3>, ...>` behind concrete out-of-line methods. The raw-pointer state split was the best measured variant; the `std::unique_ptr` variant moved too much template work back into the header.
  4. Rewrite `bb_bench.hpp` so `BB_BENCH_NAME` does not use `ThreadBenchStats<OperationLabel>` non-type template machinery in every instrumented header. A non-template `ensure_stats(const char*)` guarded by `use_bb_bench` would likely trade negligible disabled-mode runtime work for less template work in 148 TUs.
  5. Split flavor metadata from executable relation code in `eccvm_flavor.hpp` and `translator_flavor.hpp`; this is higher risk but attacks 53/39 hot TUs and large token sources, not just a single TU.
- Rejected `env/hardware_concurrency.hpp` micro-experiment: replacing the public `uint32_t` declaration with `unsigned int` and dropping `<cstdint>` built successfully, but the clean no-PCH trace regressed to `1584.1s` execute / `1128.8s` frontend / `416.6s` backend, and the tiny header still showed `32.6s` source-event time. The apparent self-heavy cost is not fixed by removing `<cstdint>`; do not keep that change.
- Rejected direct `bbapi_execute.cpp` switch experiment: replacing the remaining direct C++ `std::visit` with an index switch reduced local `bbapi_execute.cpp` instantiation work (`57.5s` -> `41.6s`) and the `bbapi named_union/variant` layer (`37.3s` -> `25.0s`), but clean no-PCH aggregate work regressed to `1507.2s` execute / `1080.0s` frontend / `391.4s` backend. A one-off switch is not worth keeping unless it also removes the public `Command`/`CommandResponse` exposure from the target.
- Rejected `bb_bench.hpp` runtime-map experiment: replacing `ThreadBenchStats<OperationLabel>` with a non-template `ensure_thread_bench_stats(const char*)` reduced `bb_bench.hpp` bytes and a little header self time, but clean no-PCH aggregate work regressed to `1527.8s` execute / `1101.3s` frontend / `389.8s` backend, and template instantiation increased to `1578.7s` inclusive / `455.5s` self across `517,820` events. Do not keep map-based BB_BENCH registration; the next win must remove broader repeated STL/field work instead of adding a new `unordered_map` path.
- Extended `scripts/template_layer_explorer.py` with source-route, context-route, and template-layer-route sections. Route strings are only built for events above the threshold (default `5000us`) so full `bb` reports stay usable. This helps distinguish "the header owns the work" from "the header merely sits above transitive template work".
- Correct trace hygiene for `ninja bb` profiling: delete `build-dir/**/CMakeFiles/**/*.json`, not just `src/**/*.json`. Test targets leave gtest/object traces under other CMake target directories and contaminate aggregate `bb` numbers if not removed.
- Rejected full `op_queue` out-of-line split. Moving `UltraEccOpsTable`/`ECCOpQueue` bodies into new implementation objects reduced local template routes (`ecc_ops_table.hpp` parent `60.2s` -> `8.9s`; `std vector` layer `165.5s` -> `127.0s`), but clean no-PCH total regressed to `1496.6s` execute / `1056.9s` frontend / `404.1s` backend because the new op-queue object introduced backend/codegen work. Do not split this whole class just to move template attribution; try narrower explicit-instantiation or representation changes instead.
- Rejected `UltraEccOpsTable::get_reconstructed` append-loop experiment. Replacing range/fill `std::vector<UltraOp>::insert` with explicit `push_back` loops removed the duplicated `std::vector<bb::UltraOp>::insert`/`_M_range_insert` entries and reduced `ecc_ops_table.hpp` parent attribution (`60.71s` -> `43.70s`), but clean no-PCH total regressed to `1594.5s` execute / `1141.7s` frontend / `414.0s` backend, and template instantiation rose to `1653.8s` inclusive / `477.5s` self across `527,014` events. Do not trade STL range-insert templates for repeated push/realloc machinery in this header.
- Kept `LookupHashTable` concrete-state split. `plookup_tables/types.hpp` no longer exposes the `std::unordered_map<std::array<fr,3>, ...>` member or hash functor; those live in `types.cpp` behind explicit copy/move/destructor/lookup methods. Same-session clean no-PCH A/B moved aggregate traced work from `1516.5s` execute / `1085.2s` frontend / `394.9s` backend to `1479.1s` / `1064.9s` / `380.3s`, and template instantiation from `1547.5s` inclusive / `447.6s` self / `518,814` events to `1457.6s` / `428.9s` / `499,151`. The targeted duplicate `LookupHashTable` unordered-map/operator[] entries disappeared; `types.hpp` parent attribution fell from `26.06s` incl / `8.59s` self to `16.62s` / `5.33s`. Tests passed: `stdlib_circuit_builders_tests`, `ultra_honk_tests`, and clean `ninja bb`.
- Rejected `common/serialize.hpp`/`serialize_msgpack.hpp` split. Moving `SERIALIZATION_FIELDS`-derived read/write bodies behind a new helper built successfully after exposing a missing direct `try_catch_shim` dependency in `msgpack_to_json.hpp`, but clean no-PCH total regressed to `1604.6s` execute / `1152.7s` frontend / `414.1s` backend, and template instantiation rose to `1668.1s` inclusive / `485.0s` self across `529,342` events. `serialize.hpp` parent attribution did not improve (`23.92s` -> `24.62s`), so its cost is not dominated by `msgpack_apply` exposure.
- Rejected `LookupHashTable` `std::unique_ptr<State>` variant. It removed the unordered-map duplication but replaced part of it with repeated `std::unique_ptr<LookupHashTable::State>` template work. The kept version uses a raw owning pointer with out-of-line special members, which is less pretty but cheaper for this header.
- Rejected `U256Codec::serialize_to_fields` insert-to-push-loop experiment. It reduced local `field_conversion.hpp` attribution (`28.05s` -> `17.10s`) and still passed focused field-conversion tests, but clean no-PCH aggregate regressed to `1521.0s` execute / `1097.6s` frontend / `387.5s` backend, with template instantiation rising to `1535.1s` inclusive / `448.3s` self across `509,137` events. This is a good example of not trusting source-header attribution alone: simpler local STL calls can increase global pending-instantiation work.
- Rejected `bb_bench.hpp` raw linked-list pointer experiment. Replacing `TimeStats::next`'s `std::unique_ptr` with manual ownership dropped local `bb_bench.hpp` attribution (`19.74s` -> `4.31s`) and the `std unique_ptr` layer (`16.41s` -> `4.82s`), but clean no-PCH aggregate still regressed to `1500.4s` execute / `1082.1s` frontend / `384.2s` backend, with template instantiation at `1484.9s` inclusive / `436.6s` self. Do not keep local template-layer wins unless the full target moves with them.
- Rejected manual `ECCVMFlavor::AllEntities::get_all` flattening. Replacing `DEFINE_COMPOUND_GET_ALL` with explicit `RefArray` construction compiled, but clean no-PCH aggregate regressed from the kept lookup split's `1479.1s` execute / `1064.9s` frontend / `380.3s` backend to `1611.5s` / `1174.8s` / `399.9s`, and template instantiation rose from `1457.6s` inclusive / `428.9s` self / `499,151` events to `1668.8s` / `492.3s` / `520,734` events. Do not hand-flatten huge entity lists in headers; it creates a larger parse/type-check surface and worsens pending instantiations.
- Rejected `EccOpsTable`/`std::vector<UltraOp>` explicit-instantiation experiment. Adding `extern template` declarations plus an `op_queue` object built only after adding a real `stdlib_circuit_builders -> op_queue` dependency edge, but clean no-PCH work regressed to `1529.0s` execute / `1106.1s` frontend / `387.5s` backend, and template instantiation rose to `1543.7s` inclusive / `452.9s` self / `512,740` events. The duplicate `std::vector<bb::UltraOp>::insert` and `_M_range_insert` entries remained at `127` TUs, so the repeated work is inline function/body parsing, not suppressible explicit instantiation.
- Rejected `TranscriptManifest` pimpl split. Moving `std::map<size_t, RoundData>` and logging bodies out of `transcript_manifest.hpp` dropped the header's local template attribution from `17.29s` incl / `5.16s` self to `1.95s` / `2.73s`, and removed the duplicated `std::map<unsigned long, TranscriptManifest::RoundData>::operator[]` entry, but clean no-PCH aggregate regressed from the kept lookup baseline `1479.1s` execute / `1064.9s` frontend / `380.3s` backend to `1517.2s` / `1097.6s` / `384.5s`. Template instantiation also regressed from `1457.6s` inclusive / `428.9s` self / `499,151` events to `1520.7s` / `445.9s` / `506,126`. Do not keep pimpls that add a new object and special members unless they reduce total work.
- Rejected `field_impl.hpp` `std::vector<bool>` scratch-mask replacement. Changing the `batch_invert` skipped mask to `std::vector<unsigned char>` reduced local `field_impl.hpp` source self attribution (`4.49s` -> `2.30s`) and slightly reduced duplicated `_Bit_iterator` instantiations, but clean no-PCH aggregate regressed from `1479.1s` execute / `1064.9s` frontend / `380.3s` backend to `1483.3s` / `1066.3s` / `382.5s`. Template instantiation rose from `1457.6s` inclusive / `428.9s` self / `499,151` events to `1476.2s` / `432.0s` / `498,055`. The `stl_bvector` cost is broader than this scratch vector; do not chase it with one-off container swaps.
- Next template-resolution avenues worth profiling:
  1. `op_queue/ecc_ops_table.hpp`: try explicit instantiation declarations for `std::vector<UltraOp>`, `std::vector<ECCVMOperation>`, and the nested subtable vectors before another body split. If that does not work, consider concrete non-template table types that avoid `EccOpsTable<T>` parsing in 126 TUs while keeping codegen limited.
  2. `eccvm_flavor.hpp` and `translator_flavor.hpp`: attack `AllEntities::get_all`/`tuplet` route work by generating flatter entity accessors or precomputed arrays for concrete flavors. This is higher-risk but now a larger target than msgpack in clean `bb`.
  3. `field_conversion.hpp`: do not micro-rewrite vector mechanics. Instead, try non-template overloads or explicit specializations for the common commitment/base-field cases so the hot transcript routes avoid repeatedly evaluating the `IsAnyOf`/array/univariate cascade.
  4. `common/serialize.hpp`: profile whether callers need full serialization templates or only byte helpers. It remains a 160+ TU parent source and may have a msgpack-like "cheap declarations, private implementation" split available.
  5. `bb_bench.hpp`: only revisit if willing to change the compile-time contract, e.g. a compile-time-disabled no-op instrumentation mode for normal builds. Runtime-map and raw-pointer variants both failed the total-work criterion.
- Kept `ExecutionTraceBlock` swap-with-empty cleanup. Replacing `clear(); shrink_to_fit();` with swapping against an empty vector avoids the `vector::shrink_to_fit` route in the header without changing the visible lifetime semantics of `free_memory()`/`free_data()`. Same-session clean no-PCH A/B moved aggregate traced work from `1527.1s` execute / `1103.3s` frontend / `388.0s` backend to `1520.9s` / `1095.3s` / `390.3s`, and template instantiation from `1550.9s` inclusive / `454.4s` self / `513,097` events to `1516.7s` / `447.2s` / `506,534`. Keep it as a small but real duplicated-STL reduction.
- Kept `CircuitSchemaInternal` split. The msgpack-backed schema struct moved from public builder headers into `circuit_schema_internal.hpp`, included only by `ultra_circuit_builder.cpp`; `circuit_builder_base.hpp` and `ultra_circuit_builder.hpp` no longer pull `barretenberg/serialize/msgpack.hpp` for ordinary builder consumers. Clean no-PCH moved from the swap baseline `1520.9s` execute / `1095.3s` frontend / `390.3s` backend to `1505.9s` / `1094.8s` / `376.8s`; template instantiation moved from `1516.7s` inclusive / `447.2s` self / `506,534` events to `1465.8s` / `434.4s` / `501,302`. `circuit_builder_base.hpp` parent attribution fell from `18.28s` incl / `6.42s` self to `11.21s` / `3.53s`, and `ultra_circuit_builder.hpp` inclusive parse dropped from `48.22s` / `5.91s` to `44.08s` / `5.54s`.
- Rejected `CircuitBuilderBaseAuxiliaryData` pimpl-style split. Moving `_tau`/`variable_names` maps behind a non-template auxiliary object and accessor methods built, but clean no-PCH work regressed to `1699.1s` execute / `1200.0s` frontend / `457.9s` backend, and template instantiation rose to `1751.8s` inclusive / `508.0s` self / `524,249` events. The builder base maps are not worth splitting unless the public API can stop touching their STL types entirely.
- Rejected `ECCVMMSMMBuilder::compute_rows` out-of-line split. Moving the large concrete body from `msm_builder.hpp` to a new `msm_builder.cpp` built after moving the op-queue dependency into the implementation, but matched clean no-PCH control after reverting measured `1576.1s` execute / `1118.8s` frontend / `419.5s` backend and `1586.1s` inclusive template work / `459.2s` self / `520,518` events, while the split measured `1914.5s` / `1370.1s` / `495.9s` and `2089.6s` / `602.8s` / `548,302`. The extra object only showed `4.2s` instantiation locally, so the regression is a good warning that moving a concrete inline body can perturb the whole clean build; do not keep it without a deeper frontend/object-level explanation.
- Latest clean no-PCH control after rejecting `msm_builder` is noisier than the earlier schema-split sample: `1576.1s` execute / `1118.8s` frontend / `419.5s` backend versus the earlier `1505.9s` / `1094.8s` / `376.8s`. Treat future decisions as same-session A/B comparisons, not single absolute samples. The accepted lookup/swap/schema changes have same-session A/B wins; the rejected experiments failed same-session A/B despite local attribution wins.
- Kept narrow `plookup_tables/types.hpp` include-prune. After the lookup split, the header no longer uses `throw_or_abort.hpp`; removing it built clean and same-session no-PCH `ninja bb` moved from `1576.1s` execute / `1118.8s` frontend / `419.5s` backend to `1529.4s` / `1096.9s` / `397.3s`. Template instantiation moved from `1586.1s` inclusive / `459.2s` self / `520,518` events to `1519.6s` / `446.4s` / `510,197`. Local `types.hpp` attribution did not improve (`17.74s` to `17.99s`), so this win came from broader include graph effects; keep measuring whole-target work.
- Kept `eccvm_builder_types.hpp` include-narrowing. The header only needs `uint256_t`, standard containers, and dependent `CycleGroup` member types, so it no longer includes the full BN254 and Grumpkin aggregate curve headers. Same-session clean no-PCH `ninja bb` moved from the include-prune control `1529.4s` execute / `1096.9s` frontend / `397.3s` backend to `1494.4s` / `1063.9s` / `395.4s`; template instantiation moved from `1519.6s` inclusive / `446.4s` self / `510,197` events to `1455.6s` / `426.2s` / `495,859`. The main improvements were in op-queue/ECCVM routes (`ecc_ops_table.hpp` `65.17s` -> `61.12s`, `ecc_op_queue.hpp` `34.08s` -> `31.16s`) and the field/curve layer (`366.18s` -> `348.88s`). This is the best current kind of change: remove semantic include volume from many TUs without adding new objects.
- Rejected op-queue aggregate-curve include narrowing. Replacing `curve::BN254`/`curve::Grumpkin` aliases in `ecc_ops_table.hpp`, `ecc_op_queue.hpp`, `eccvm_row_tracker.hpp`, and `msm_builder.hpp` with direct `fr`/`fq`/`g1` spelling built successfully after fixing a hidden `msm_builder.hpp` transitive dependency, but clean no-PCH regressed from `1494.4s` execute / `1063.9s` frontend / `395.4s` backend to `1881.4s` / `1311.0s` / `524.1s`. Template instantiation rose from `1455.6s` inclusive / `426.2s` self / `495,859` events to `1890.2s` / `538.8s` / `551,852`; `ecc_ops_table.hpp` parent attribution increased from `61.12s` to `72.33s`. Do not replace these aggregate curve aliases piecemeal; the direct spelling perturbs field/group template work across the whole build.
- Kept AVM mock-proof lightweight path. `mock_verifier_inputs.cpp` no longer includes `vm2/constraining/flavor.hpp` just to build fake AVM proof fields; the AVM proof-shape constants are hardcoded in a tiny header and checked against `AvmFlavor` in `mock_verifier_inputs.test.cpp`. Same-session clean no-PCH A/B was noisy but positive: reverted control `1636.6s` execute / `1166.2s` frontend / `431.9s` backend and `1640.7s` template inclusive / `475.8s` self / `524,145` events, versus lightweight `1626.4s` / `1160.1s` / `427.8s` and `1613.1s` / `471.5s` / `523,973`. The target TU dropped from `18.06s` execute / `15.56s` frontend to `10.80s` / `8.15s`, and `vm2/constraining/flavor.hpp` disappeared from the `bb` parent-source list. This pattern is good for one-off mock/test helpers: move heavy real-flavor validation to tests and keep production helper TUs on cheap proof-shape data.
- Kept AVM recursion declaration include split. `dsl/acir_format/avm2_recursion_constraint.hpp` now forward-declares the recursive verifier output and the input constraint instead of including `honk_recursion_constraint.hpp`, `recursion_constraint.hpp`, and `bigfield.hpp`; the non-AVM stub includes the complete output type only in its `.cpp`. Clean no-PCH after the split measured `1486.3s` execute / `1061.8s` frontend / `390.2s` backend and `1452.1s` template inclusive / `426.4s` self / `495,752` events. Local wins were `vm2_stub/avm2_recursion_constraint.cpp` around `8.15s -> 5.96s` execute, plus smaller drops in `recursion_constraint.cpp`, `api_acir.cpp`, and `acir_format.cpp`. Recursion/mock dsl test filter passed: `65` passed, `5` skipped.
- Kept `PairingPointsTagging` out-of-line split. `pairing_points_tagging.hpp` no longer exposes `<algorithm>` or `BB_ASSERT` bodies through `circuit_builder_base.hpp`; method bodies and special members live in the existing `circuit_builder_base.cpp` object, so no new TU was added. Clean no-PCH `ninja bb` moved from the committed request-header split baseline `1411.9s` execute / `1005.6s` frontend / `374.6s` backend to `1404.0s` / `1001.3s` / `371.2s`. Template instantiation moved from `1287.4s` inclusive / `391.2s` self / `459,632` events to `1277.8s` / `390.1s` / `455,924`; the `std vector` layer dropped from `157.5s` to `151.2s`. Tests passed: clean no-PCH `ninja bb`, `stdlib_circuit_builders_tests`, and `ultra_honk_tests` (`283` passed, `8` skipped). `stdlib_primitives_tests` still has an unrelated `FrCodec` compile error in `field_conversion.test.cpp`, so it could not be used as the pairing-points test runner.
