[phases.setup]
nixPkgs = ["python310", "git"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "chmod +x setup_reference_repo.sh",
  "chmod +x app.py"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120"
