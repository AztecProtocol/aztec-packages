You are being tested as part of the ClaudeBox CI system. Confirm you are running correctly by performing these steps in order:

1. Run `echo "ClaudeBox is alive"` via Bash
2. Run `git status` to show the worktree state
3. Run `pwd` to confirm your working directory
4. List the first 10 files/directories with `ls | head -10`
5. Print a short summary confirming everything works

If any arguments were passed to you, echo them back: $ARGS
