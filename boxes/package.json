{
  "name": "@aztec/boxes",
  "packageManager": "yarn@4.0.2",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "yarn workspaces foreach -A -p -j unlimited -v run compile",
    "build": "yarn workspaces foreach -A -p -j unlimited -v run build"
  },
  "workspaces": [
    "blank",
    "blank-react"
  ],
  "bin": {
    "create-aztec": "npx.js"
  },
  "devDependencies": {
    "@inquirer/confirm": "^3.0.0",
    "@inquirer/input": "^2.0.0",
    "@inquirer/select": "^2.0.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "node-pty": "^1.0.0",
    "tiged": "^2.12.6"
  }
}
