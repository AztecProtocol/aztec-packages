[1mdiff --git a/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr b/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[1mindex e99f9d2333..6d1bb52fec 100644[m
[1m--- a/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[1m+++ b/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[36m@@ -97,7 +97,7 @@[m [mmod test {[m
     use crate::context::PrivateContext;[m
     use crate::hash::hash_args;[m
     use crate::messages::delivery::OnchainDeliveryMode;[m
[31m-    use crate::protocol::{address::AztecAddress, traits::{FromField, ToField}};[m
[32m+[m[32m    use crate::protocol::{address::AztecAddress, hash::compute_siloed_nullifier, traits::{FromField, ToField}};[m
     use crate::test::helpers::test_environment::TestEnvironment;[m
     use super::{compute_constrained_msg_nullifier, constrain_secret_and_emit_nullifier, VALIDATE_HANDSHAKE_SELECTOR};[m
     use std::test::OracleMock;[m
[36m@@ -197,6 +197,8 @@[m [mmod test {[m
         let index: u32 = 3;[m
 [m
         env.private_context(|context| {[m
[32m+[m[32m            let _ = OracleMock::mock("aztec_prv_isNullifierPending").returns(false).times(1);[m
[32m+[m
             constrain_secret_and_emit_nullifier(context, registry, sender, recipient, secret, false, index);[m
 [m
             assert_current_nullifier_emitted(context, sender, recipient, secret, index);[m
[36m@@ -204,10 +206,13 @@[m [mmod test {[m
             assert_eq(context.nullifier_read_requests.len(), 1);[m
 [m
             let read_request = context.nullifier_read_requests.get(0);[m
[31m-            assert_eq(read_request.contract_address, context.this_address());[m
[32m+[m[32m            assert_eq(read_request.contract_address, AztecAddress::zero());[m
             assert_eq([m
                 read_request.inner.inner,[m
[31m-                compute_constrained_msg_nullifier(sender, recipient, secret, index - 1),[m
[32m+[m[32m                compute_siloed_nullifier([m
[32m+[m[32m                    context.this_address(),[m
[32m+[m[32m                    compute_constrained_msg_nullifier(sender, recipient, secret, index - 1),[m
[32m+[m[32m                ),[m
             );[m
         });[m
     }[m
