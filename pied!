[1mdiff --git a/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr b/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[1mindex 65035375cc..84d432bc27 100644[m
[1m--- a/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[1m+++ b/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[36m@@ -122,9 +122,9 @@[m [mpub fn compute_constrained_msg_nullifier([m
 /// Computes the constrained-delivery discovery log tag for an `(app_siloed_secret, index)` pair.[m
 ///[m
 /// Collapses the secret and index into a single raw tag and domain-separates it with[m
[31m-/// `DOM_SEP__CONSTRAINED_MSG_LOG_TAG`. This mirrors the recipient-side derivation in the PXE (`siloedTagFor`): the[m
[31m-/// recipient recomputes the same tag to discover the message, and the protocol silos the emitted log's first field by[m
[31m-/// the emitting contract address, completing the match.[m
[32m+[m[32m/// `DOM_SEP__CONSTRAINED_MSG_LOG_TAG`. This mirrors the recipient-side `Tag.compute` / `SiloedTag.compute` derivation[m
[32m+[m[32m/// in the PXE: the recipient recomputes the same tag to discover the message, and the protocol silos the emitted log's[m
[32m+[m[32m/// first field by the emitting contract address, completing the match.[m
 pub fn compute_constrained_log_tag(app_siloed_secret: Field, index: u32) -> Field {[m
     compute_log_tag([m
         poseidon2_hash([app_siloed_secret, index as Field]),[m
[1mdiff --git a/yarn-project/aztec/scripts/templates/counter/contract/src/main.nr b/yarn-project/aztec/scripts/templates/counter/contract/src/main.nr[m
[1mindex c879cb48b4..be027752e4 100644[m
[1m--- a/yarn-project/aztec/scripts/templates/counter/contract/src/main.nr[m
[1m+++ b/yarn-project/aztec/scripts/templates/counter/contract/src/main.nr[m
[36m@@ -24,7 +24,7 @@[m [mpub contract Counter {[m
     #[initializer][m
     #[external("private")][m
     fn constructor(initial_value: u128, owner: AztecAddress) {[m
[31m-        // Delivers the note to the recipient onchain with provable correctness.[m
[32m+[m[32m        // Delivers the note to the recipient onchain so they can discover and decrypt it.[m
         // Without delivery, the recipient can't find or decrypt the note.[m
         self.storage.counters.at(owner).add(initial_value).deliver([m
             MessageDelivery::onchain_unconstrained(),[m
