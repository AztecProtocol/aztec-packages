[1mdiff --git a/noir-projects/noir-contracts/contracts/account/simulated_ecdsa_account_contract/src/main.nr b/noir-projects/noir-contracts/contracts/account/simulated_ecdsa_account_contract/src/main.nr[m
[1mindex 0544820909..6a354765ba 100644[m
[1m--- a/noir-projects/noir-contracts/contracts/account/simulated_ecdsa_account_contract/src/main.nr[m
[1m+++ b/noir-projects/noir-contracts/contracts/account/simulated_ecdsa_account_contract/src/main.nr[m
[36m@@ -27,9 +27,9 @@[m [mpub contract SimulatedEcdsaAccount {[m
     // Does NOT use #[initializer] so that the macro does not inject[m
     // assert_initialization_matches_address_preimage_private, which would fail during kernelless[m
     // simulation because the stub instance has a different initialization hash than the real account.[m
[31m-    // Emits the same shape of side effects as the real constructor (one nullifier for[m
[31m-    // SinglePrivateImmutable initialization, one note hash for the key note, and one private[m
[31m-    // log tied to that note hash) so that gas estimation produces accurate results.[m
[32m+[m[32m    // Emits side effects matching the *shape* of the real EcdsaKAccount / EcdsaRAccount constructor (the[m
[32m+[m[32m    // key-note storage plus the constrained-delivery handshake bootstrap it triggers) so that kernelless gas[m
[32m+[m[32m    // estimation produces accurate results. See the body for the per-effect breakdown and the coupling this creates.[m
     #[external("private")][m
     fn constructor(_signing_pub_key_x: [u8; 32], _signing_pub_key_y: [u8; 32]) {[m
         // Safety: Random seeds are only used to produce dummy side effects that match the shape of[m
[36m@@ -56,6 +56,30 @@[m [mpub contract SimulatedEcdsaAccount {[m
             BoundedVec::from_array(dummy_log),[m
             self.context.side_effect_counter,[m
         );[m
[32m+[m
[32m+[m[32m        // The real constructor delivers the key note with `MessageDelivery::onchain_constrained()`, and a[m
[32m+[m[32m        // first send bootstraps a handshake: a nested call to `HandshakeRegistry::non_interactive_handshake`[m
[32m+[m[32m        // inserts a handshake note (note hash + initialization nullifier) and emits two logs (the note-delivery[m
[32m+[m[32m        // log and the recipient discovery log), and the constrained send emits a chain nullifier. We reproduce[m
[32m+[m[32m        // only the *shape* of those side effects so gas estimation stays accurate without running the real[m
[32m+[m[32m        // handshake.[m
[32m+[m[32m        //[m
[32m+[m[32m        // This couples the stub to constrained delivery's emitted shape: if the handshake bootstrap or[m
[32m+[m[32m        // chain-nullifier emission changes, update these dummies to match or the gas-parity assertion in[m
[32m+[m[32m        // e2e_kernelless_simulation.test.ts will fail.[m
[32m+[m
[32m+[m[32m        // Handshake note hash and its initialization nullifier.[m
[32m+[m[32m        self.context.push_note_hash(seed + 4);[m
[32m+[m[32m        self.context.push_nullifier_unsafe(seed + 5);[m
[32m+[m
[32m+[m[32m        // Chain nullifier emitted by the constrained send.[m
[32m+[m[32m        self.context.push_nullifier_unsafe(seed + 6);[m
[32m+[m
[32m+[m[32m        // Handshake note-delivery log and recipient discovery log.[m
[32m+[m[32m        let dummy_handshake_note_log: [Field; MESSAGE_CIPHERTEXT_LEN] = [seed + 7; MESSAGE_CIPHERTEXT_LEN];[m
[32m+[m[32m        self.context.emit_private_log_unsafe(seed + 8, BoundedVec::from_array(dummy_handshake_note_log));[m
[32m+[m[32m        let dummy_handshake_discovery_log: [Field; MESSAGE_CIPHERTEXT_LEN] = [seed + 9; MESSAGE_CIPHERTEXT_LEN];[m
[32m+[m[32m        self.context.emit_private_log_unsafe(seed + 10, BoundedVec::from_array(dummy_handshake_discovery_log));[m
     }[m
 [m
     // @dev: If you globally change the entrypoint signature don't forget to update account_entrypoint.ts[m
[1mdiff --git a/noir-projects/noir-contracts/contracts/account/simulated_schnorr_account_contract/src/main.nr b/noir-projects/noir-contracts/contracts/account/simulated_schnorr_account_contract/src/main.nr[m
[1mindex 9d3959ed94..daa4403506 100644[m
[1m--- a/noir-projects/noir-contracts/contracts/account/simulated_schnorr_account_contract/src/main.nr[m
[1m+++ b/noir-projects/noir-contracts/contracts/account/simulated_schnorr_account_contract/src/main.nr[m
[36m@@ -29,9 +29,9 @@[m [mpub contract SimulatedSchnorrAccount {[m
     // Does NOT use #[initializer] so that the macro does not inject[m
     // assert_initialization_matches_address_preimage_private, which would fail during kernelless[m
     // simulation because the stub instance has a different initialization hash than the real account.[m
[31m-    // Emits the same shape of side effects as the real constructor (one nullifier for[m
[31m-    // SinglePrivateImmutable initialization, one note hash for the key note, and one private[m
[31m-    // log tied to that note hash) so that gas estimation produces accurate results.[m
[32m+[m[32m    // Emits side effects matching the *shape* of the real SchnorrAccount constructor (the key-note storage[m
[32m+[m[32m    // plus the constrained-delivery handshake bootstrap it triggers) so that kernelless gas estimation[m
[32m+[m[32m    // produces accurate results. See the body for the per-effect breakdown and the coupling this creates.[m
     #[external("private")][m
     fn constructor(_signing_pub_key_x: Field, _signing_pub_key_y: Field) {[m
         // Safety: Random seeds are only used to produce dummy side effects that match the shape of[m
[36m@@ -58,6 +58,30 @@[m [mpub contract SimulatedSchnorrAccount {[m
             BoundedVec::from_array(dummy_log),[m
             self.context.side_effect_counter,[m
         );[m
[32m+[m
[32m+[m[32m        // The real constructor delivers the key note with `MessageDelivery::onchain_constrained()`, and a[m
[32m+[m[32m        // first send bootstraps a handshake: a nested call to `HandshakeRegistry::non_interactive_handshake`[m
[32m+[m[32m        // inserts a handshake note (note hash + initialization nullifier) and emits two logs (the note-delivery[m
[32m+[m[32m        // log and the recipient discovery log), and the constrained send emits a chain nullifier. We reproduce[m
[32m+[m[32m        // only the shape of those side effects so gas estimation stays accurate without running the real[m
[32m+[m[32m        // handshake.[m
[32m+[m[32m        //[m
[32m+[m[32m        // This couples the stub to constrained delivery's emitted shape: if the handshake bootstrap or[m
[32m+[m[32m        // chain-nullifier emission changes, update these dummies to match or the gas-parity assertion in[m
[32m+[m[32m        // e2e_kernelless_simulation.test.ts will fail.[m
[32m+[m
[32m+[m[32m        // Handshake note hash and its initialization nullifier.[m
[32m+[m[32m        self.context.push_note_hash(seed + 4);[m
[32m+[m[32m        self.context.push_nullifier_unsafe(seed + 5);[m
[32m+[m
[32m+[m[32m        // Chain nullifier emitted by the constrained send.[m
[32m+[m[32m        self.context.push_nullifier_unsafe(seed + 6);[m
[32m+[m
[32m+[m[32m        // Handshake note-delivery log and recipient discovery log.[m
[32m+[m[32m        let dummy_handshake_note_log: [Field; MESSAGE_CIPHERTEXT_LEN] = [seed + 7; MESSAGE_CIPHERTEXT_LEN];[m
[32m+[m[32m        self.context.emit_private_log_unsafe(seed + 8, BoundedVec::from_array(dummy_handshake_note_log));[m
[32m+[m[32m        let dummy_handshake_discovery_log: [Field; MESSAGE_CIPHERTEXT_LEN] = [seed + 9; MESSAGE_CIPHERTEXT_LEN];[m
[32m+[m[32m        self.context.emit_private_log_unsafe(seed + 10, BoundedVec::from_array(dummy_handshake_discovery_log));[m
     }[m
 [m
     // @dev: If you globally change the entrypoint signature don't forget to update account_entrypoint.ts[m
