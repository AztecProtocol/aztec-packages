[1mdiff --git a/yarn-project/txe/src/oracle/tagging_secret_strategy.test.ts b/yarn-project/txe/src/oracle/tagging_secret_strategy.test.ts[m
[1mindex fdd24cacfc..8a38811e6c 100644[m
[1m--- a/yarn-project/txe/src/oracle/tagging_secret_strategy.test.ts[m
[1m+++ b/yarn-project/txe/src/oracle/tagging_secret_strategy.test.ts[m
[36m@@ -4,9 +4,7 @@[m [mimport { DEFAULT_TAGGING_SECRET_STRATEGY, type TaggingSecretStrategy } from '@az[m
 import { AztecAddress } from '@aztec/stdlib/aztec-address';[m
 import { AppTaggingSecretKind } from '@aztec/stdlib/logs';[m
 [m
[31m-import { toSingle } from '../utils/encoding.js';[m
 import { makeResolveTaggingSecretStrategyHook } from './tagging_secret_strategy.js';[m
[31m-import { callTxeHandler } from './txe_oracle_registry.js';[m
 [m
 describe('makeResolveTaggingSecretStrategyHook', () => {[m
   it('returns undefined when no TXE strategy is configured', () => {[m
[36m@@ -37,39 +35,6 @@[m [mdescribe('makeResolveTaggingSecretStrategyHook', () => {[m
     );[m
   });[m
 [m
[31m-  it('deserializes the per-mode TXE oracle setter', async () => {[m
[31m-    const received = await callTxeHandler({[m
[31m-      oracle: 'aztec_txe_setTaggingSecretStrategies',[m
[31m-      inputs: [[m
[31m-        // Unconstrained mode: some(arbitrary-secret with point (5, 6)).[m
[31m-        toSingle(1),[m
[31m-        toSingle(2),[m
[31m-        toSingle(5),[m
[31m-        toSingle(6),[m
[31m-        // Constrained mode: some(interactive-handshake).[m
[31m-        toSingle(1),[m
[31m-        toSingle(4),[m
[31m-        toSingle(0),[m
[31m-        toSingle(0),[m
[31m-      ],[m
[31m-      handler: ([unconstrainedStrategy, constrainedStrategy]) => {[m
[31m-        if (!constrainedStrategy.isSome()) {[m
[31m-          throw new Error('Expected a constrained-mode tagging secret strategy');[m
[31m-        }[m
[31m-        expect(constrainedStrategy.value).toEqual({ type: 'interactive-handshake' });[m
[31m-        if (!unconstrainedStrategy.isSome()) {[m
[31m-          throw new Error('Expected an unconstrained-mode tagging secret strategy');[m
[31m-        }[m
[31m-        const strategy = unconstrainedStrategy.value;[m
[31m-        if (strategy.type !== 'arbitrary-secret') {[m
[31m-          throw new Error(`Expected an arbitrary-secret strategy, got '${strategy.type}'`);[m
[31m-        }[m
[31m-        expect(strategy.secret.equals(new Point(new Fr(5), new Fr(6)))).toBe(true);[m
[31m-      },[m
[31m-    });[m
[31m-[m
[31m-    expect(received.values).toEqual([]);[m
[31m-  });[m
 });[m
 [m
 function makeRequest(deliveryMode: AppTaggingSecretKind) {[m
