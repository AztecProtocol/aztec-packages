[1mdiff --git a/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.test.ts b/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.test.ts[m
[1mindex e2ee4f4028..9af02967de 100644[m
[1m--- a/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.test.ts[m
[1m+++ b/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.test.ts[m
[36m@@ -23,7 +23,8 @@[m [mdescribe('CircuitRecorder', () => {[m
 [m
 describe('recordCall without an active recording', () => {[m
   // Under concurrent use the shared recorder can be reset (recording === undefined) between an oracle returning[m
[31m-  // and its recordCall() bookkeeping. recordCall() must record best-effort and never throw into the execution path.[m
[32m+[m[32m  // and its recordCall() bookkeeping. recordCall() must not throw into the execution path; dropped recorder data is[m
[32m+[m[32m  // acceptable until recorder state is isolated.[m
   const expectedEntry = { name: 'loadCapsule', inputs: [['0x01']], outputs: ['0x02'], time: 5, stackDepth: 0 };[m
 [m
   it('MemoryCircuitRecorder.recordCall() returns the entry without pushing to an absent recording', async () => {[m
[1mdiff --git a/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.ts b/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.ts[m
[1mindex bbd13edbb1..86e2f79088 100644[m
[1m--- a/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.ts[m
[1m+++ b/yarn-project/simulator/src/private/circuit_recording/circuit_recorder.ts[m
[36m@@ -15,7 +15,7 @@[m [mexport type OracleCall = {[m
   // oracle calls performed after a foreign call (which is itself an oracle call)[m
   // We keep track of the stack depth in this variable to ensure the recorded oracle[m
   // calls are correctly associated with the right circuit.[m
[31m-  // This is only use as a debugging tool[m
[32m+[m[32m  // This is only used as a debugging tool[m
   stackDepth: number;[m
 };[m
 [m
[36m@@ -228,7 +228,8 @@[m [mexport class CircuitRecorder {[m
       time,[m
       stackDepth,[m
     };[m
[31m-    // Under concurrent use the shared recording can be reset mid-flight; record best-effort and never throw.[m
[32m+[m[32m    // Recording is opportunistic under overlapping simulations; recorder failures must not affect execution.[m
[32m+[m[32m    // TODO: Isolate recorder state per simulation so concurrent executions cannot drop or misattribute calls.[m
     this.recording?.oracleCalls.push(entry);[m
     return Promise.resolve(entry);[m
   }[m
