[1mdiff --git a/noir-projects/aztec-nr/aztec/src/oracle/notes.nr b/noir-projects/aztec-nr/aztec/src/oracle/notes.nr[m
[1mindex fc46a1d047..09d6ca6985 100644[m
[1m--- a/noir-projects/aztec-nr/aztec/src/oracle/notes.nr[m
[1m+++ b/noir-projects/aztec-nr/aztec/src/oracle/notes.nr[m
[36m@@ -194,9 +194,9 @@[m [munconstrained fn get_next_app_tag_as_sender_oracle(_sender: AztecAddress, _recip[m
 ///[m
 /// Unlike [`get_next_app_tag_as_sender`], which derives the full tag from a `(sender, recipient)` pair that only the[m
 /// simulator knows the directional secret for, the constrained-delivery flow has the app contract holding the[m
[31m-/// app-siloed shared secret directly. The app computes the tag itself as[m
[31m-/// `compute_log_tag(poseidon2_hash([app_siloed_secret, index]), DOM_SEP__CONSTRAINED_MSG_LOG_TAG)`, so the simulator[m
[31m-/// only needs to hand out a fresh per-secret index.[m
[32m+[m[32m/// app-siloed shared secret directly. The app computes the tag itself but it can't tell what the next index should be,[m
[32m+[m[32m/// so the simulator is the one that needs to hand out a fresh per-secret index. However, the constrained caller[m
[32m+[m[32m/// must still check that the returned index is valid.[m[41m [m
 ///[m
 /// As with the unconstrained variant, the simulator persists the index increment only if the tagged log is found in[m
 /// an actual block - a reverting transaction can otherwise cause the sender to accidentally skip indices and later[m
[1mdiff --git a/yarn-project/pxe/src/contract_function_simulator/oracle/private_execution_oracle.ts b/yarn-project/pxe/src/contract_function_simulator/oracle/private_execution_oracle.ts[m
[1mindex 91aaa1425d..98543dc03a 100644[m
[1m--- a/yarn-project/pxe/src/contract_function_simulator/oracle/private_execution_oracle.ts[m
[1m+++ b/yarn-project/pxe/src/contract_function_simulator/oracle/private_execution_oracle.ts[m
[36m@@ -208,8 +208,8 @@[m [mexport class PrivateExecutionOracle extends UtilityExecutionOracle implements IP[m
   /**[m
    * Returns the next app tag for a given sender and recipient pair (unconstrained delivery).[m
    *[m
[31m-   * The simulator owns the directional tagging secret (derived via ECDH from `(sender, recipient, contract)`), so it[m
[31m-   * computes the full siloed tag here and hands the opaque value back to the caller.[m
[32m+[m[32m   * The simulator computes the directional tagging secret (derived via ECDH from `(sender, recipient, contract)`),[m
[32m+[m[32m   * as well as the full siloed tag, and hands the opaque value back to the caller.[m
    *[m
    * @param sender - The address sending the log[m
    * @param recipient - The address receiving the log[m
[36m@@ -239,7 +239,7 @@[m [mexport class PrivateExecutionOracle extends UtilityExecutionOracle implements IP[m
   /**[m
    * Returns the next sender-side index for a constrained-delivery app-siloed shared secret.[m
    *[m
[31m-   * Unlike the unconstrained variant, the simulator does not own the secret: it was supplied by the calling contract[m
[32m+[m[32m   * Unlike the unconstrained variant, the simulator does not compute the secret: it was supplied by the calling contract[m
    * (which retrieved it from an onchain handshake registry). The simulator only acts as a per-secret index counter.[m
    * The caller computes the onchain tag itself.[m
    *[m
