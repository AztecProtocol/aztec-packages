[1mdiff --git a/yarn-project/pxe/src/storage/backwards_compatibility_tests/schema_tests.ts b/yarn-project/pxe/src/storage/backwards_compatibility_tests/schema_tests.ts[m
[1mindex c2dee92928..748604fc31 100644[m
[1m--- a/yarn-project/pxe/src/storage/backwards_compatibility_tests/schema_tests.ts[m
[1m+++ b/yarn-project/pxe/src/storage/backwards_compatibility_tests/schema_tests.ts[m
[36m@@ -147,7 +147,7 @@[m [mexport const SCHEMA_TESTS: readonly SchemaTest[] = [[m
       const handshakeSecretStore = new HandshakeSecretStore(kvStore);[m
 [m
       const jobId = 'fixture-job';[m
[31m-      // Hand-rolled non-curve points are fine here — the store does not validate curve membership when serializing.[m
[32m+[m[32m      // Hand-rolled non-curve points are fine here. The store does not validate curve membership when serializing.[m
       // We pick fixed coordinates so the on-disk hash keys are stable across runs.[m
       await handshakeSecretStore.setHandshakeSecret(new Point(new Fr(2n), new Fr(3n), false), jobId);[m
       await handshakeSecretStore.setHandshakeSecret(new Point(new Fr(5n), new Fr(7n), false), jobId);[m
