[1mdiff --git a/aztec-up/bin/0.0.1/install b/aztec-up/bin/0.0.1/install[m
[1mindex 28483aaeb2..c617328808 100755[m
[1m--- a/aztec-up/bin/0.0.1/install[m
[1m+++ b/aztec-up/bin/0.0.1/install[m
[36m@@ -221,21 +221,20 @@[m [mfunction install_native_symlinks {[m
   set -euo pipefail[m
   local tool[m
   for tool in forge cast anvil chisel nargo noir-profiler; do[m
[31m-    [ -e "$version_internal_bin_path/$tool" ] || continue[m
[32m+[m[32m    if [ ! -e "$version_internal_bin_path/$tool" ]; then[m
[32m+[m[32m      echo "Error: expected bundled binary '$tool' missing from $version_internal_bin_path" >&2[m
[32m+[m[32m      exit 1[m
[32m+[m[32m    fi[m
     ln -sfn "../internal-bin/$tool" "$version_bin_path/aztec-$tool"[m
   done[m
 }[m
 [m
[31m-# For each @aztec-owned bin in node_modules/.bin, expose an aztec-prefixed[m
[31m-# symlink in bin/ (renaming bare names like `bb`, `pxe`, `txe`). The `aztec`[m
[31m-# bin is skipped here -- install_aztec_wrapper writes a wrapper script for it.[m
[31m-# We do NOT mirror npm bins into internal-bin: tools that the aztec workflow[m
[31m-# spawns by basename are expected to resolve their dependencies via absolute[m
[31m-# paths or pinned env vars (e.g. compile.ts uses `findBbBinary()` from[m
[31m-# @aztec/bb.js which returns an absolute path; profile_*.ts honours `BB` and[m
[31m-# `PROFILER_PATH` env vars). If a future tool starts cross-calling a sibling[m
[31m-# by basename, fix that tool to resolve an absolute bundled path instead of[m
[31m-# broadening internal-bin here.[m
[32m+[m[32m# Npm bins are NOT mirrored into internal-bin. The contract for any TS tool[m
[32m+[m[32m# that needs to spawn a sibling binary is to resolve an absolute bundled[m
[32m+[m[32m# path itself (e.g. via @aztec/bb.js findBbBinary(), or a BB-style env[m
[32m+[m[32m# var) -- never by basename + PATH lookup. If you find yourself wanting to[m
[32m+[m[32m# add an npm bin to internal-bin so a child process can find it on PATH,[m
[32m+[m[32m# fix the caller instead of broadening this list.[m
 function install_npm_bin_symlinks {[m
   set -euo pipefail[m
   local npm_bin_dir="$version_path/node_modules/.bin"[m
