RemoveTest
├── when address not in the set
│   └── it returns false
├── when validator is in the set
│   ├── it returns true
│   ├── it decreases the length
│   ├── it updates the snapshot for that index
│   └── it maintains historical values correctly
├── when validator removing an index larger than the current length
│   └── it reverts
└── when removing multiple validators
    ├── it maintains correct order of remaining validators
    └── it updates snapshots correctly for each removal
