{
  "scripts": {
    "build": "yarn clean && yarn generate && yarn formatting:fix",
    "generate": "./scripts/generate-types.sh",
    "clean": "rm -rf .tsbuildinfo ./artifacts ./codegenCache.json"
  }
}
