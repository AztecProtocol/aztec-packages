{
  "scripts": {
    "build": "yarn clean && yarn generate",
    "generate": "./scripts/generate-types.sh && yarn formatting:fix",
    "clean": "rm -rf .tsbuildinfo ./artifacts ./codegenCache.json"
  }
}
