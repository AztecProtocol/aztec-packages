// TODO(#12333): Implement upgrade tests
// Also, needed to add this file so that `aztec/src/**/*.test.ts` as invoked in `yarn-project/bootstrap.sh` is not empty.
describe('Upgrade', () => {
  it('should upgrade', () => {
    expect(true).toBe(true);
  });
});
