# File Store

Read/write access to files on local or remote stores behind a common `FileStore` interface. Supported backends: Google Cloud Storage (`gs://`), AWS S3 (`s3://`), local filesystem (`file://`), and read-only HTTP(S).
