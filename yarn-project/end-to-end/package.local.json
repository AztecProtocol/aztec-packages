{
  "scripts": {
    "build": "yarn clean && tsc -b && webpack",
    "test": "NODE_NO_WARNINGS=1 node --experimental-vm-modules --testTimeout=300000 --forceExit --runInBand ../node_modules/.bin/jest src/fixtures"
  }
}
