//use dep::private_kernel_lib::{KernelCircuitPublicInputs};
//use dep::private_kernel_init::{PrivateKernelInputsInit};
//use dep::std::unsafe::zeroed;

//fn main(input: PrivateKernelInputsInit) -> distinct pub KernelCircuitPublicInputs {
    // zeroed out, so we do not need to have verbose default methods
//    let zeroed : KernelCircuitPublicInputs = zeroed();
//    zeroed
//}