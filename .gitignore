dest
node_modules
.cache
.pnp.cjs
.pnp.loader.mjs
build/
.idea
cmake-build-debug
.terraform*
terraform.tfstate*
.bootstrapped
.tsbuildinfo
bench-out
dumped-avm-circuit-inputs

# Local Netlify folder
.netlify

.graphite*
.DS_Store

# tmux
tmux-client-*.log
.supermavenignore

# parallel
joblog.txt
.logged_test_flakes

docs/.yarn/install-state.gz
docs/docs/protocol-specs/public-vm/gen/

__pycache__

# scarb / cairo-language-server cache dropped at the repo root by the Cairo
# tooling used in the zkvm spike (zkvm/backends/stwo-cairo). Anchored to root
# so it doesn't shadow nested target/ handling (see zkvm/.gitignore).
/target/

*.local.md
.claude/settings.local.json
