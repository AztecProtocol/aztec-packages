import React from 'react';

export const Fee_Components = () => (
  <p>
The Gas settings have two components: Data availability and L2 cost

Each of these have a gas value and a fee-per-gas of each component.
</p>
)


export const Gas_Settings = () => (
  <p>
    Gas Settings used in transactions specify gas limits and maximum fee rates (fees-per-gas)
  </p>
)

export const Spec_Placeholder = () => (
  <p>The design and implementation have largely changed since the original specification, and these docs will soon be updated to reflect the latest implementation.
</p>
);

