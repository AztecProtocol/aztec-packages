## Standalone Functions

### note_is_nullified

```rust
note_is_nullified();
```

Takes no parameters.

### note_is_not_nullified

```rust
note_is_not_nullified();
```

Takes no parameters.

### nullifier_inclusion

```rust
nullifier_inclusion();
```

Takes no parameters.

### nullifier_inclusion_fails

```rust
nullifier_inclusion_fails();
```

Takes no parameters.

