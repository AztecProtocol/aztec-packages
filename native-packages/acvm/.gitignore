# The @aztec/acvm-sim TS client package is fully code-generated by bootstrap.sh
# (`ipc-codegen --package`); it is regenerated on every build and never committed.
ts/
# Rust build artifacts.
rust/target/
# Codegen output (Rust server dispatch + types), regenerated by bootstrap.sh.
**/generated/
node_modules
*.log
