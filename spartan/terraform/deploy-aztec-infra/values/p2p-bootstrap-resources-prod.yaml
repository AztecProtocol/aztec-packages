nodeSelector:
  local-ssd: "false"
  node-type: "network"

node:
  resources:
    requests:
      cpu: "1"
      memory: "2Gi"

persistence:
  enabled: true

statefulSet:
  enabled: true
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 4Gi
