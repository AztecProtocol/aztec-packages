bot:
  nodeSelector:
    local-ssd: "false"
    node-type: "network"
    cores: "2"
    pool: "spot"

  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: cloud.google.com/gke-spot
                operator: Exists
              - key: cloud.google.com/gke-nodepool
                operator: NotIn
                values:
                  - aztec-gke-private-32core-spot

  tolerations:
    - key: "cloud.google.com/gke-spot"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"

  node:
    resources:
      requests:
        cpu: "0.25"
        memory: "1Gi"
      limits:
        cpu: "0.5"
        memory: "2Gi"
