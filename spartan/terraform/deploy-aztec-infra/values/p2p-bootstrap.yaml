nameOverride: "node"

replicaCount: 1

podDisruptionBudget:
  enabled: true
  minAvailable: 1

env:
  OTEL_SERVICE_NAME: "boot-node"

node:
  startCmd:
    - --p2p-bootstrap

service:
  p2p:
    enabled: true
    nodePortEnabled: false
  admin:
    enabled: false
  headless:
    enabled: false
