nodeSelector:
  local-ssd: "false"
  node-type: "network"

node:
  resources:
    requests:
      cpu: "0.1"
      memory: "0.5Gi"
    limits:
      cpu: "0.5"
      memory: "1Gi"

persistence:
  enabled: false

statefulSet:
  enabled: false
