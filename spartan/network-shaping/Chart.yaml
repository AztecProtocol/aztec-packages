apiVersion: v2
name: network-shaping
description: Network shaping for spartan using chaos-mesh
type: application
version: 0.1.0
appVersion: "1.0.0"

# Reference local chaos-mesh chart
dependencies:
  - name: chaos-mesh
    version: "0.1.0"
    repository: "file://../chaos-mesh"
    condition: chaos-mesh.enabled
    enabled: false