telemetry:
  enabled: true

bootNode:
  debug: "libp2p:gossipsub*"

validator:
  debug: "libp2p:gossipsub*"
  replicas: 64

  resources:
    requests:
      memory: "512Mi"
  validator:
    disabled: false

bootNode:
  debug: "libp2p:gossipsub*"
  validator:
    disabled: true
