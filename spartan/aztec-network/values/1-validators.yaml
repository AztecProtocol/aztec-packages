telemetry:
  enabled: true

validator:
  replicas: 1
  validator:
    disabled: false

bootNode:
  validator:
    disabled: true

ethereum:
  execution:
    resources:
      requests:
        memory: "2Gi"
        cpu: "1"
    storageSize: "10Gi"
  beacon:
    resources:
      requests:
        memory: "2Gi"
        cpu: "1"
    storageSize: "10Gi"
  validator:
    resources:
      requests:
        memory: "2Gi"
        cpu: "1"
    storageSize: "10Gi"
