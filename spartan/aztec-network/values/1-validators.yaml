telemetry:
  enabled: true

validator:
  replicas: 1
  sequencer:
    minTxsPerBlock: 0
  validator:
    disabled: false

ethereum:
  execution:
    resources:
      requests:
        cpu: "1"
    storageSize: "10Gi"
  beacon:
    resources:
      requests:
        cpu: "1"
    storageSize: "10Gi"
  validator:
    resources:
      requests:
        cpu: "1"
    storageSize: "10Gi"
