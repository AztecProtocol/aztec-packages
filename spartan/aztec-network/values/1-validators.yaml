telemetry:
  enabled: true

validator:
  replicas: 1
  validator:
    disabled: false

bootNode:
  validator:
    disabled: true

ethereum:
  execution:
    resources:
      requests:
        cpu: "1"
    storageSize: "10Gi"
  beacon:
    resources:
      requests:
        cpu: "1"
    storageSize: "10Gi"
  validator:
    resources:
      requests:
        cpu: "1"
    storageSize: "10Gi"
