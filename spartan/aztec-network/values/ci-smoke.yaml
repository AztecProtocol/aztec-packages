bootNode:
  resources:
    requests:
      cpu: "200m"

fullNode:
  replicas: 1
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"

validator:
  resources:
    requests:
      cpu: "200m"

proverNode:
  resources:
    requests:
      cpu: "200m"

bot:
  enabled: false

ethereum:
  acceleratedTestDeployments: true
  execution:
    resources:
      requests:
        cpu: "200m"
      limits:
        cpu: "200m"
  beacon:
    resources:
      requests:
        cpu: "200m"
      limits:
        cpu: "200m"
  validator:
    resources:
      requests:
        cpu: "200m"
      limits:
        cpu: "200m"

proverAgent:
  resources:
    requests:
      cpu: "200m"

proverBroker:
  resources:
    requests:
      cpu: "200m"
