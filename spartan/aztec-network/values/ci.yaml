aztec:
  slotDuration: 24
  epochDuration: 4
  proofSubmissionWindow: 8

ethereum:
  blockTime: 8
  execution:
    resources:
      requests:
        cpu: "200m"
      limits:
        cpu: "200m"
  beacon:
    resources:
      requests:
        cpu: "200m"
      limits:
        cpu: "200m"
  validator:
    resources:
      requests:
        cpu: "200m"
      limits:
        cpu: "200m"

telemetry:
  enabled: true

validator:
  replicas: 8
  resources:
    requests:
      cpu: "200m"
  validator:
    disabled: false
  sequencer:
    enforceTimeTable: false

bot:
  followChain: "PENDING"
  enabled: true
  txIntervalSeconds: 1

bootNode:
  validator:
    disabled: true
  resources:
    requests:
      cpu: "200m"

proverAgent:
  resources:
    requests:
      cpu: "200m"

proverBroker:
  resources:
    requests:
      cpu: "200m"

proverNode:
  resources:
    requests:
      cpu: "200m"
