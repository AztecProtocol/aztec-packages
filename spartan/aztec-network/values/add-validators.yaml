aztec:
  testAccounts: true
  slotDuration: 12
  epochDuration: 4
  proofSubmissionWindow: 128

ethereum:
  blockTime: 4

bootNode:
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"

fullNode:
  replicas: 1
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"

validator:
  resources:
    requests:
      memory: "512Mi"
      cpu: "200m"

proverNode:
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"

pxe:
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"

bot:
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"
  enabled: false

ethereum:
  acceleratedTestDeployments: true
  execution:
    resources:
      requests:
        memory: "2Gi"
        cpu: "200m"
      limits:
        memory: "2Gi"
        cpu: "200m"
  beacon:
    resources:
      requests:
        memory: "2Gi"
        cpu: "200m"
      limits:
        memory: "2Gi"
        cpu: "200m"
  validator:
    resources:
      requests:
        memory: "2Gi"
        cpu: "200m"
      limits:
        memory: "2Gi"
        cpu: "200m"

proverAgent:
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"

proverBroker:
  resources:
    requests:
      memory: "2Gi"
      cpu: "200m"
