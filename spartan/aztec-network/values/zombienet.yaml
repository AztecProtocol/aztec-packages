telemetry:
  enabled: true

network:
  setupL2Contracts: false

bot:
  enabled: false

pxe:
  enabled: false

faucet:
  enabled: false

validator:
  replicas: 3
  sequencer:
    minTxsPerBlock: 0
    maxTxsPerBlock: 0
  validator:
    disabled: false
  resources:
    requests:
      cpu: "1"

bootNode:
  validator:
    disabled: true
  resources:
    requests:
      cpu: "1"

proverBroker:
  resources:
    requests:
      memory: "2Gi"
      cpu: "1"

proverAgent:
  resources:
    requests:
      memory: "2Gi"
      cpu: "1"

ethereum:
  execution:
    resources:
      requests:
        memory: "1Gi"
        cpu: "0.5"
  beacon:
    resources:
      requests:
        memory: "1Gi"
        cpu: "0.5"
  validator:
    resources:
      requests:
        memory: "1Gi"
        cpu: "0.5"
