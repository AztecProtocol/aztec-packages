apiVersion: v1
kind: Service
metadata:
  name: alpha-testnet-monitor
  namespace: staging-testnet-monitoring
  labels:
    app: alpha-testnet-monitor
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: alpha-testnet-monitor
