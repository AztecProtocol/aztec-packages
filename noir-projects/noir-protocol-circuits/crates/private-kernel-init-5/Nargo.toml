[package]
name = "private_kernel_init_5"
type = "bin"
authors = [""]
compiler_version = ">=0.18.0"

[dependencies]
private_kernel_lib = { path = "../private-kernel-lib" }
types = { path = "../types" }
