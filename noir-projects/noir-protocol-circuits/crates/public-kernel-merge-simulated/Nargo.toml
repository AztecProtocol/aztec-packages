[package]
name = "public_kernel_merge_simulated"
type = "bin"
authors = [""]
compiler_version = ">=0.18.0"

[dependencies]
types = { path = "../types" }
public_kernel_lib = { path = "../public-kernel-lib" }
