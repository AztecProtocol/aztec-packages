[package]
name = "blob"
type = "lib"
authors = [""]
compiler_version = ">=0.30.0"

[dependencies]
bigint = { tag = "v0.5.4", git = "https://github.com/noir-lang/noir-bignum" }
types = { path = "../types" }
